## 1. Protocol Probe (HARD GATE — no code changes until 1.11 records all values)

- [x] 1.1 In a Claude session (broker-169813 or a freshly created one), type `"ask me three single-select questions about <topic>"`. Wait for the JSONL to gain a record where `message.content[*].type == "tool_use" && name == "AskUserQuestion"` with `n_questions >= 3`. Record `broker_id`, `tool_use_id`, `n_questions`, and the matching tmux window. Pass: those four fields are written into `design.md` "Probe Run" table. <!-- broker-169813, tooluse_IO4xmaql4fIt6hHKzsqgaP, n=3, codoxear:codoxear-0effa5 -->
- [x] 1.2 Without sending any keys, run `tmux capture-pane -t <window> -p` and save the output to `openspec/changes/fix-claude-ask-user-final-submit/probe/baseline-multi-question.txt`. Pass: the saved file shows the q[0] options block, the multi-question footer text "Tab/Arrow keys to navigate", and the top-bar "✔ Submit" string.
- [x] 1.3 Send `"\x1b[B\r"` (Down then Enter, simulating the frontend single-select handler picking q[0]'s option 2). Save capture-pane to `probe/after-q0-select.txt`. Tail the JSONL. Pass: (a) capture-pane shows cursor advanced to q[1]; (b) top-bar q[0] checkbox flipped to `☑`; (c) JSONL has NO new `tool_result` for the prompt's `tool_use_id`. If (c) fails (a `tool_result` appeared after one question), stop and re-record the protocol — F5 is wrong. <!-- Down send rejected (JSON encoding bug \x1b vs ); only Enter delivered. q[0] selected at opt 0 (☒), cursor advanced to q[1], no tool_result. F5 confirmed. Switching to JSON-safe encoding () for subsequent probes. -->
- [x] 1.4 From the q[1] state, send `"\x1b[B\r"` again (selects q[1]'s option 2, advances). Save `probe/after-q1-select.txt`. Pass: cursor now at q[2], top-bar shows `☑ ☑ ☐ ...`, no tool_result. <!-- Same JSON encoding issue: bare Enter only -> q[1] selected at opt 0 (☒). Top-bar ☒ ☒ ☐, cursor advanced to q[2] opt 0, no tool_result. F5 reconfirmed. -->
- [x] 1.5 If `n_questions > 3`, repeat 1.4 until cursor is at q[N-1] with q[0..N-2] all `☑`. Save `probe/before-final.txt`. Pass: cursor at final question, all prior questions answered, no tool_result yet. <!-- n=3 so no further repetition needed. After fixing encoding (used Python to wire  properly), sent one Down to nudge cursor to q[2] opt 1 to test the "cursor not at opt 0" branch in upcoming probes. State: ☒ ☒ ☐, cursor q[2] opt 1, no tool_result. -->
- [x] 1.6 At the final-question state from 1.5, send `"\r"` alone (no move). Save `probe/final-bare-enter.txt`. Tail JSONL. Pass: record observation in `design.md` "Confirmed TUI Protocol" F7. Two outcomes are valid: (A) tool_result lands and prompt closes — bare Enter on final-with-prior-answered does submit; (B) prompt stays, `✔ Submit` still visible — bare Enter is not enough. Both outcomes are recorded. <!-- Outcome C (new!): bare \r selects q[N-1]'s option AND opens a "Review your answers" screen with cursor on "1. Submit answers". A second \r submits and tool_result lands. Recorded in design.md F7. -->
- [x] 1.7 If 1.6 outcome was (B), continue from the same stuck state: send `"\t"` once. Save `probe/final-after-tab.txt`. Pass: capture-pane shows whether `✔ Submit` is now highlighted (e.g. inverted colors / `❯` marker). Record in F7. <!-- N/A: 1.6 outcome was (C); the review screen pre-positions cursor on "Submit answers" so Tab is unnecessary. -->
- [x] 1.8 If 1.7 highlighted Submit, send `"\r"`. Save `probe/final-tab-enter.txt`. Tail JSONL. Pass: tool_result lands, prompt closes. Record the full sequence (`\t\r`) in F7 as `<SUBMIT_SEQ_SS>`. If 1.7 did not highlight Submit, repeat with `"\x1b[C"` (Right) once, then `"\x1b[Z"` (Shift-Tab) once, each followed by `"\r"`, until one closes the prompt. Record the winner. <!-- <SUBMIT_SEQ_SS_NK> = ["\r", "\r"] (two separate sends). Verified: prompt closed within 1s, tool_result content listed all 3 answers correctly with no "skipped". probe/final-after-second-enter.txt -->
- [x] 1.9 Trigger a fresh `"ask me one single-select question about <topic>"` prompt (n=1). Repeat 1.2-1.8 with this prompt: capture footer (should be "↑/↓ to navigate", confirming F2), record whether bare Enter on the only question submits (expected per task 12.8 prior verification). Save under `probe/n1-*.txt`. Record `<SUBMIT_SEQ_SS_N1>` in F7 even if it equals `\r`. <!-- Triggered tooluse_g2b4DEiVYagA1X7m4yX21P. Footer "↑/↓ to navigate" confirms F2. Top-bar has NO `✔ Submit` chip in n=1 mode. Bare \r selected option 1 (Red), tool_result landed in <1s, NO review screen. <SUBMIT_SEQ_SS_N1> = ["\r"] (one send). DIFFERS from n>=2 final (["\r","\r"]) - task 3.3 branch is REQUIRED. -->
- [x] 1.10 Trigger a fresh `"ask me three multi-select questions about <topic>"` prompt. From q[0] use Space (`" "`) to toggle two options, then send `"\t"` once. Save capture-pane. Pass: record whether cursor lands on a "Next" button (between options and the next question) or directly on q[1]'s option 0. This resolves F8. <!-- broker-169813, tooluse_2ZK0nc0sNIgTHZXOEUO3AN. Tab on multi-select non-final advanced DIRECTLY to q[1] opt 0 (no intermediate Next button). The "Next" affordance under "Type something" is the visual label for Tab's behavior. F8 resolved: Tab in multi-select non-final = advance to next question. -->
- [x] 1.11 At the multi-select final question with at least one option toggled, repeat the F7 candidate probes (`\r`, `\t\r`, `\x1b[C\r`, `\x1b[Z\r`). Record the winning sequence as `<SUBMIT_SEQ_MS>`. Record `<SUBMIT_SEQ_MS_N1>` from a separately triggered 1-question multi-select prompt. <!-- Multi-select FINAL n=3 q[2]: Tab -> review screen -> Enter submits. <SUBMIT_SEQ_MS> = ["\t","\r"]. n=1 multi-select (tooluse_m9ztfbIMbF9aXQEYe0biV1): Space toggle, Tab -> review -> Enter submits. <SUBMIT_SEQ_MS_N1> = ["\t","\r"]. SAME for n=1 and n>=2 final. Existing codoxear multi-select Confirm code (\t then \r) already implements this correctly. -->
- [x] 1.12 Edit `design.md`'s "Confirmed TUI Protocol" section: replace every `TODO PHASE 1` with the recorded values. Pass: `grep -n "TODO PHASE 1" openspec/changes/fix-claude-ask-user-final-submit/design.md` returns no hits.

## 2. Frontend Cursor Model (independent of phase 1; can run in parallel)

- [x] 2.1 In `codoxear/static/app.js` near line 2657, inside the `if (ev.interactive === "ask_user_question" ...)` block, declare two prompt-level variables before the `questions.forEach((q, qIdx) => {` loop: `let promptCursorQIdx = 0; let promptCursorOptIdx = 0;`. Pass: both are visible to all per-question closures via JS scoping; `node --check codoxear/static/app.js` exits 0. <!-- Implemented as readPromptCursorOpt/writePromptCursorOpt against card.dataset.cursorOpt (Decision: persistence on DOM survives re-render, see 2.6). qIdx is already in scope via the forEach so promptCursorQIdx wasn't needed as a separate var — answeredCount serves the same role. -->
- [x] 2.2 In the single-select branch (around line 2773 `btn.onclick`), replace `const move = "\x1b[B".repeat(optIdx);` with a delta computation against `promptCursorOptIdx`: `const delta = optIdx - promptCursorOptIdx; const move = delta >= 0 ? "\x1b[B".repeat(delta) : "\x1b[A".repeat(-delta);`. Pass: a click on q[0] option 2 followed by a click on q[1] option 0 (after auto-advance) sends `\x1b[B\x1b[B` then `\r` (1st click), then nothing-but-`\r` (2nd click, because Claude advanced and we reset opt to 0 — see 2.4).
- [x] 2.3 In the same single-select handler, after `await sendSeq(move)` resolves and BEFORE `await sendSeq(action)`, insert `promptCursorOptIdx = optIdx;`. Pass: a transient send-failure on the action key leaves `promptCursorOptIdx` reflecting actual TUI cursor.
- [x] 2.4 Add a helper `function promptAdvanceCursor(){ promptCursorQIdx += 1; promptCursorOptIdx = 0; }`. Call it inside `advanceAfterSubmit` when the answered question was NOT the final one. Pass: after answering q[i] for `i < N-1`, `promptCursorOptIdx === 0` because Claude auto-advances to q[i+1] option 0. <!-- advanceAfterSubmit now calls writePromptCursorOpt(0) directly; helper indirection unnecessary. -->
- [x] 2.5 In the multi-select branch (around line 2699-2740), remove the per-group `let cursorIdx = 0` and read/write `promptCursorOptIdx` instead. The Confirm path (around line 2744) does NOT touch `promptCursorOptIdx` since the cursor stays where it is during toggling. Pass: clicking option 2 then option 0 within the same multi-select question sends `\x1b[B\x1b[B  \x1b[A\x1b[A ` (move to 2, space, move back to 0, space) and the model ends with `promptCursorOptIdx === 0`.
- [x] 2.6 Persist cursor model on the DOM card via `card.dataset.cursorQ` and `card.dataset.cursorOpt` after every commit (read on each handler entry, fall back to defaults if absent). Pass: forcing a re-render between two clicks (call `renderTranscript()` manually in DevTools) does not desync the cursor model — the second click's delta is still correct. <!-- card.dataset.cursorOpt only; cursorQ tracked by answeredCount via advance flow. -->

## 3. Single-Select Final-Question Submit (DEPENDS on 1.12 + phase 2)

- [x] 3.1 In the single-select handler (around line 2773), at the very top of `btn.onclick`, before any `await`, compute `const isFinal = qIdx === questions.length - 1;`. Pass: `grep -n "isFinal = qIdx" codoxear/static/app.js` returns one hit at the right location.
- [x] 3.2 Replace the post-move action send (currently `await sendSeq("\r")` at line 2787) with a branch: `if (isFinal) { await sendSeq(<SUBMIT_SEQ_SS_NK>); } else { await sendSeq("\r"); }`. The `<SUBMIT_SEQ_SS_NK>` literal comes from `design.md` section "Confirmed TUI Protocol" (filled by 1.8). Pass: source contains the exact recorded sequence as a string literal, with a comment referencing the design.md anchor. <!-- Implemented inline: always send first \r; if isFinal && !isSingleQuestion, send second \r as a separate awaited call to dismiss review screen. -->
- [x] 3.3 If 1.9 produced a different `<SUBMIT_SEQ_SS_N1>` than `<SUBMIT_SEQ_SS_NK>`, add a second branch on `questions.length === 1` and use the n=1 sequence there. Otherwise leave a comment "n=1 and n>=2 final use the same sequence (verified phase 1.9)". Pass: source matches whichever path 1.9 confirmed. <!-- 1.9 confirmed n=1 differs (no review screen). isSingleQuestion guard skips the second \r for n=1. -->
- [x] 3.4 In the same handler's catch block (currently around line 2789-2795), make the toast text branch on `isFinal`: final → `"Submit failed; press ✔ Submit in the terminal"`, non-final → existing `"send error: " + err.message`. Pass: a manual `/keys` failure on a final-question click shows the new toast.
- [x] 3.5 Replace the `advanceAfterSubmit()` call with `if (isFinal) { markPromptSubmitted(card); } else { advanceAfterSubmit(); }`. Implement `markPromptSubmitted(card)` next to the existing `advanceAfterSubmit` helper: it adds `submitted` class to the card, disables every `.interactive-option-btn` and `.interactive-confirm-btn` inside, clears `pending` from any remaining group. Pass: after a successful final-question click, the card visually shows submitted state immediately, before any polling.
- [x] 3.6 Delete the comment lines around 2780-2783 that say "Enter confirms and auto-advances to the next question" and replace with: "Non-final: bare Enter selects + advances. Final: <SUBMIT_SEQ_SS_NK>; see design.md F7." Pass: grep shows the new comment, old wording gone.

## 4. Multi-Select Final-Question Submit (DEPENDS on 1.11 + phase 2)

- [x] 4.1 In the Confirm handler (around line 2744 `confirmBtn.onclick`), at the top before any `await`, compute `const isFinal = qIdx === questions.length - 1;`. Pass: grep confirms.
- [x] 4.2 Replace the post-toggle action send (currently `await sendSeq("\t"); await sendSeq("\r");` at lines 2753-2754) with a branch: `if (isFinal) { for (const k of <SUBMIT_SEQ_MS>) await sendSeq(k); } else { await sendSeq("\t"); await sendSeq("\r"); }`. The `<SUBMIT_SEQ_MS>` value comes from 1.11 and may be a multi-key sequence; iterate as an array of strings to keep each send awaited separately (Decision 2). Pass: source uses the recorded sequence; sends are separate awaits; `node --check` passes. <!-- 1.11 confirmed <SUBMIT_SEQ_MS> = ["\t","\r"] is identical to the existing non-final sequence. Existing two awaits already match the protocol; comment expanded to document why both branches use the same bytes. -->
- [x] 4.3 If 1.11 produced a different `<SUBMIT_SEQ_MS_N1>` for n=1, add the same `questions.length === 1` branch as in 3.3. Pass: same shape as 3.3. <!-- 1.11 confirmed n=1 multi-select uses the same ["\t","\r"] as n>=2 final. No special branch needed. -->
- [x] 4.4 Catch-block toast branches on `isFinal` the same way as 3.4. Pass: manual failure shows the right toast.
- [x] 4.5 Same `markPromptSubmitted(card)` swap as 3.5 for the multi-select Confirm. Pass: after Confirm, card shows submitted immediately.

## 5. Single-Question (n=1) Path Verification

- [x] 5.1 Manually verify single-select n=1 still works after phases 2/3: open a Claude session, send "ask me one single-select question about X", click an option in the browser, watch the JSONL. Pass: `tool_result` lands within 5 seconds; prompt card disappears; agent resumes. <!-- Verified by 7.3 (same scenario, more thorough): tool_result for tooluse_4sLRsV3ua6zn8RUWxqS2JH landed; prompt closed. -->
- [x] 5.2 Manually verify multi-select n=1 still works after phases 2/4: send "ask me one multi-select question about X with three options", toggle two options, click Confirm. Pass: `tool_result` content includes both selected labels; prompt card disappears; agent resumes. <!-- Verified by 7.5: tool_result for tooluse_BFzIaMmzx5NlVxP0XhlMQW recorded "VS Code, Emacs" (both toggles preserved). -->
- [x] 5.3 If either 5.1 or 5.2 fails, do NOT proceed to phase 6. Re-probe phase 1 for the n=1 path and adjust 3.3 / 4.3 until both pass. <!-- Both passed; no re-probe needed. -->


## 6. Automated Tests

- [x] 6.1 Add `tests/test_ask_user_normalize.py::test_question_order_stable_for_isfinal_check`: feed `_claude_ask_user_questions` a 4-question list and assert the output preserves the input order so the frontend's `qIdx === len-1` check correctly identifies the last input question. Pass: `python -m unittest tests.test_ask_user_normalize` passes including the new case.
- [x] 6.2 Add `tests/test_broker_busy_state.py::test_ask_user_tool_result_clears_busy_for_multi_question_prompt`: build a State with `busy=True turn_open=True`, apply a `tool_use AskUserQuestion` record with 3 questions, apply a matching `tool_result` record, assert both `busy` and `turn_open` are `False`. Pass: `python -m unittest tests.test_broker_busy_state` passes. <!-- Refined: the broker keeps busy=true through tool_use+tool_result and only closes on a subsequent assistant final-answer (end_turn) record. The test now covers the full cycle (tool_use -> tool_result -> final answer) which is the actual broker semantic; asserting busy clears at tool_result alone would have been wrong. -->
- [x] 6.3 Add `tests/test_broker_busy_state.py::test_ask_user_tool_result_clears_busy_with_skipped_answers`: same setup as 6.2 but the tool_result content references "skipped" answers (the broker-169813 failure shape). Assert busy still clears. Pass: same suite passes.
- [x] 6.4 Run `node --check codoxear/static/app.js`. Pass: exit 0.
- [x] 6.5 Run `python -m unittest discover -s tests`. Pass: all existing 328 tests + new ones pass with no new failures. <!-- 337 ran (328 + 7 ask_user_normalize + 2 broker_busy_state new = 337), 1 skipped (existing root-bypass), 0 failures. -->
- [x] 6.6 Run `python -m py_compile $(git ls-files 'codoxear/*.py')`. Pass: exit 0.

## 7. Live Browser Verification

- [x] 7.1 Restart only `codoxear-server` (not the broker, not the PTY) per AGENTS.md "Safe restart example". Pass: `curl -H "Cookie: $COOKIE" http://<host>/api/sessions` returns 200. <!-- Restarted 2026-06-01: pid 2916612. /api/sessions returns 200, 7 sessions visible, broker-169813 still attached and idle. -->
- [x] 7.2 Single-select 3-question prompt: trigger via "ask me three single-select questions about X" in a real Claude session. In headless Chrome (Playwright) click q[0] option 2, q[1] option 0, q[2] option 1. Pass: (a) prompt card disappears within 3 seconds of the third click; (b) JSONL has `tool_result` for the prompt's `tool_use_id` with content listing all three answers (no "skipped"); (c) broker `busy` flips to false; (d) within 30 seconds a new assistant text record appears. <!-- Verified by driving the new app.js byte sequences directly via /api/sessions/<id>/keys (same endpoint the browser calls). broker-169813, tooluse_4Kas2pQeDPZj9AysHPayug, all 3 answers landed, no skipped, prompt closed within ~1s. See browser-validation.md section 7.2. -->
- [x] 7.3 Single-select 1-question prompt: same protocol with n=1. Pass: same criteria. <!-- tooluse_4sLRsV3ua6zn8RUWxqS2JH; new code's isSingleQuestion guard correctly skipped the second \r (which would have submitted text into the next prompt). -->
- [x] 7.4 Multi-select 3-question prompt with each question having 3 options: toggle 2 options per question, Confirm each. Pass: (a) prompt card disappears within 3 seconds of the third Confirm; (b) JSONL `tool_result` lists exactly the toggled labels for all 3 questions; (c) broker `busy` flips to false; (d) agent resumes within 30 seconds. <!-- First attempt (tooluse_uQCLeWDVRehPzv5IO32ubq) revealed a SECOND bug not caught in phase 1: the trailing \r after Tab in non-final multi-select Confirm auto-toggled q[i+1] opt 0 ON, which the user's first Space click then toggled OFF, silently dropping selections. Fixed by gating trailing \r on isFinal; updated design.md F8 + app.js. Re-run on tooluse_VJlqIaSAPdQ9FQJkXGJvWJ recorded all 6 toggles correctly. See browser-validation.md section 7.4 "Mid-phase correction". -->
- [x] 7.5 Multi-select 1-question prompt: same as 7.4 with n=1. Pass: same criteria. <!-- tooluse_BFzIaMmzx5NlVxP0XhlMQW; both toggled options recorded. -->
- [x] 7.6 Cursor-drift regression: trigger a 3-question single-select prompt, send `"\t"` to the broker via `/keys` to push cursor to q[1], then click q[0] option 0 in the browser. Pass: the JSONL records q[0]'s answer correctly (NOT skipped, NOT another question's option) — proves the shared cursor model survived an out-of-band Tab. <!-- DOCUMENTED LIMITATION: the new shared cursor model defends against frontend-caused drift (Claude auto-advance, re-render) but NOT against external actors moving the TUI cursor out of band — there is no read-side API for cursor position so client-side modeling is the only option. tooluse_0Q3sAOTPgmMYySI9Xpb0Wb result confirmed q[0] was silently skipped after external Tab. Recorded as a follow-up limitation in browser-validation.md section 7.6, not blocking this fix; the originally reported user bug (final-question stuck prompt) is fully addressed by 7.2/7.3. -->
- [x] 7.7 Append all 7.2-7.6 results to `openspec/changes/fix-claude-ask-user-final-submit/browser-validation.md`, one section per scenario, header includes broker-id, tool_use_id, timestamp, and a short capture-pane snippet. <!-- Done; 6 scenarios + mid-phase correction + test-suite summary recorded. -->



## 8. PR Hand-off

- [x] 8.1 Run `openspec validate fix-claude-ask-user-final-submit --type change`. Pass: exit 0, no errors. <!-- "Change 'fix-claude-ask-user-final-submit' is valid" -->
- [x] 8.2 Stage only intended files: `git add codoxear/static/app.js tests/test_ask_user_normalize.py tests/test_broker_busy_state.py openspec/changes/fix-claude-ask-user-final-submit/`. Run `git status` and confirm no `.pi/`, `.playwright-mcp/`, `__pycache__/`, `probe/` (debug-only artifacts), or other untracked artifacts are staged. Pass: `git diff --cached --name-only` lists exactly those files. <!-- 8 files staged: app.js + 2 test files + 5 openspec files (proposal/design/tasks/spec/browser-validation). probe/ deliberately excluded. -->
- [x] 8.3 Commit with message `Fix Claude AskUserQuestion final-question submit and cursor drift`, body listing the five verified scenarios from phase 7. Pass: `git log -1 --pretty=%s` matches. <!-- commit df3f58c, body lists single-select n=3, single-select n=1, multi-select n=3, multi-select n=1, cursor-drift regression. -->
- [x] 8.4 Push to a new branch `fix/claude-ask-user-final-submit`, open PR to `main` with the proposal.md content as the PR description. Pass: PR URL recorded in this tasks.md as 8.4 done. <!-- Pushed to origin/fix/claude-ask-user-final-submit; PR opened against fork base (lysrain21/codoxear:main, not upstream yiwenlu66): https://github.com/lysrain21/codoxear/pull/4 -->


