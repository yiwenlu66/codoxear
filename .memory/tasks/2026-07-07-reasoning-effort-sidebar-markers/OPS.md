# OPS

- 2026-07-07T13:52:00Z — Selected next bounded product slice after accepted CC unknown-model token clearing. Mechanism: `codoxear/static/app.js` maps sidebar `effortMark` only for `xhigh`, `high`, `medium`, and `low`; `max`, `minimal`, and `off` fall through to an empty marker. Current launch defaults and backend paths already support CC `max` and Pi `off`/`minimal`, so the remaining gap is display truth in sidebar rows, not launch semantics. No functional edits yet.
- 2026-07-07T14:05:25Z — Accepted reasoning-effort sidebar marker lifecycle. Functional commit `f59086a` replaced the sidebar inline marker ternary with `REASONING_EFFORT_MARKERS`, preserving `xhigh/high/medium/low` as `X/H/M/L` and adding `max/minimal/off` as `M+/m/–`; CSS classes for the new values were added with no backend changes. Proof commit `937e09e` records local focused pytest (`42 passed`), local full pytest (`1814 passed, 134 subtests`), Docker focused tests (`42 passed`), Docker smoke, and browser evidence on port 19421: seven fake sidecar/socket sessions rendered `M+`, `m`, `–`, `X`, `H`, `M`, `L` in desktop and mobile sidebar rows with expected classes/titles and no overflow. Review commit `eb7da3b` records clean-room ACCEPT with no blockers; non-blocking notes were marker width asymmetry, `||` fallback style, CSS test scope, and source-regex fragility.
