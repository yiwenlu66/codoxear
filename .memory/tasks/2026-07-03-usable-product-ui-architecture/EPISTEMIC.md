# Epistemic model

## Phenomenon
Codoxear is moving from test-green refactor recovery to user-perspective usability plus cleaner architecture. Browser evidence remains the decisive signal: pytest green previously missed live-route and load-order failures.

## Terminology
`Workbench` is an internal certification/planning category only. Product claims and user-facing communication must name the actual surface: sessions, transcript, files/editor, repository status/diff, attachments, mobile controls, diagnostics, settings, notifications.

## Current justified claims
- The real-backend browser path is usable for the core Pi flow: create session, send/response, queue/drain, interrupt, transcript search, file viewer, and mobile live view were proven in Docker sandbox 19083 before the current architecture tranche.
- First-run/empty-state UX is improved: sidebar hint, centered main CTA, muted disabled composer, clearer backend tab label, and desktop toast pill were browser-verified and committed.
- New Session dialog state has a dedicated controller authority (`CodoxearNewSession`): provider/model + reasoning moved in `ecb934b`; cwd/recent-cwd, resume candidate state, and worktree/tmux UI sync moved in `31a08b0`.
- Script-load order is now an explicit invariant: `app_launch.js < app_display.js < app_new_session.js < app_dom.js`. Pass 2 initially violated this because `app_new_session.js` gained a hard `CodoxearDisplay` dependency while loading before display; browser showed blank body with `hasDisplay=true`, `hasNewSession=false`. The order fix preserves fail-loud semantics.
- Pass-2 browser verification succeeded after the order fix: cwd input `/workspace` opens recent-cwd menu; resume menu loads prior session candidates and selection updates the label; a throwaway git repo in the sandbox shows the worktree field, toggling enables the branch input and changes start text; Pi start path creates a live session (`broker-255`). Evidence screenshots: d29, d30; raw observations in OPS.
- Internal monkeypatch seams are shrinking. Converted clusters: message routes, transcript export, pending-log idle, file inspect, session resume, launch defaults, unattended sweep. The mechanism is consistent: tests now call route/coordinator/impl seams with injected deps instead of patching server module globals or constructing incomplete managers.

## Open work
- Current release-candidate checkpoint is `c3693df` on `recovery/product-gaps`. Voice/settings/notifications is accepted and committed; the broad sidebar/swipe extraction attempted after that checkpoint was discarded and the tree restored clean. This negative evidence rules out “next extractable cluster” as the planner.
- Next work is whole-product certification of `c3693df` as a composed local-agent web remote: one isolated Docker deployment, desktop and mobile browser, at least one real Pi session, and the changed controllers exercised together on one live page (launch, send/queue/interrupt, transcript/search/navigation, diagnostics/recovery, unattended, voice/settings, file/git flows).
- After product-critical certification, resolve backend parity scope for Codex and Claude Code real sessions if credentials/environment allow; otherwise record the release boundary explicitly.
- Fix only evidence-backed failures. Extraction remains allowed when the defect mechanism is split authority or tangled state, but extraction is an intervention, not the roadmap. Known bounded non-product debt remains `tests/test_file_upload.py` server-global monkeypatching.

## Ruled out
- "Tests alone prove usability": falsified by live `/api/sessions` 500s, failed-launch UX bugs, and the pass-2 script-order blank page. Browser and real-server evidence are mandatory.
- Soft-loading `app_new_session.js` without `CodoxearDisplay`: rejected. The dependency is real; load order must satisfy it rather than hiding the violation.


## Acceptance judgment after clean-room review
Clean-room critic c6109f6e accepted the current tranche with no blockers. The accepted mechanism is: New Session state now has a single controller authority and correct fail-loud script order; browser evidence proves the load-order contract from the user's perspective; converted tests exercise real coordinators with injected external boundaries rather than server-global monkeypatches; validation covers local pytest, Docker test/smoke, clean browser, and independent review.

Non-blocking backlog from review: dispose the resume debounce timer on dialog close to avoid one hidden trailing request; replace/drop the vacuous `__codoxearLoadError` probe; continue reducing residual source-text assertions; continue remaining app-shell extractions beyond New Session.


## New iteration observations
- Browser scout after accepted checkpoint found a new polish/impaired-affordance issue: failed-launch rows disable attach/queue/send actions correctly, but leave the main composer textarea editable with `Enter your instructions here`. This does not allow delivery because buttons are disabled, but it misleads users into typing into a session that cannot receive messages. Fix should project sendability/readiness into the composer input state/placeholder, not just the action buttons.

### Resolution (2026-07-04, in-place on recovery/product-gaps)
- Root mechanism confirmed: `enqueueComposerText` is blocked by the same predicates as `sendText` (`launchFailed`, `orphan_recovery`, `queue_recovery`, `commit_unknown_send`, plus `!selected`). So whenever the send button is structurally blocked, typing into the composer cannot deliver anywhere via send OR enqueue. The textarea's enabled state was therefore a pure false affordance.
- Fix: added `syncComposerState()` in app.js, driven from the tail of `syncSendButtonState()` (which owns every sendability transition: session select, send start/end, recovery changes). It disables `#msg` and rewrites the `#msgPh` overlay text + textarea `aria-label`/`title` to the specific reason (`Select a session to send` / `Failed launch cannot receive messages` / `Resolve the unknown send before sending` / `Missing session can only be reviewed` / `Review preserved queued recovery items before sending`), defaulting to `Enter your instructions here` when live.
- `disabled` chosen over `readOnly` for mobile keyboard ergonomics: a disabled textarea cannot receive focus and will not raise the soft keyboard on iOS/Android, whereas `readOnly` can still grab focus and pop the keyboard on some platforms. Disabled also gives a clear greyed visual (paired with `.composer textarea:disabled { opacity: 0.5; cursor: not-allowed; }` in app.css).
- Transient `sending`/busy states are deliberately NOT projected into the composer (only the structural block is). This preserves the queue/send-choice path during a live turn and respects the constraint not to disable the composer during ordinary busy/queueable states.
- Queue/recovery button paths untouched; existing toasts preserved.
- Tests: added tests/test_composer_sendability_source.py (4 cases). Existing send/queue/attach/tooltip/mobile-zoom source sentinels still pass unchanged.


## Follow-up state
- The former `__codoxearLoadError` check is no longer vacuous after `3b762f5`: the page installs a real early error/unhandled-rejection recorder and renders a visible load-failure panel if bootstrap leaves `#root` empty. Browser evidence on sandbox 19088 proved normal load keeps the sentinel null and synthetic fallback rendering works.
- New Session resume debounce cleanup is now tied to dialog close, eliminating the critic's trailing hidden request mechanism.
- Queue orchestration extracted into `CodoxearQueue` (`codoxear/static/app_queue.js`, loaded before app.js, after CodoxearSessionHelpers + CodoxearModal). `createQueueController(options)` owns all queue state (timers, mutation locks, pending deletes, draft texts, last-edit/submit-busy/viewer sid+items, modal return-focus) and the queue button submit-state projection, enqueue/delete/move/update decision logic, viewer show/hide modal behavior, and rendered list. Pure helpers (normalizeQueueItems + launch/recovery predicates) come from CodoxearSessionHelpers; modal focus/isolation from CodoxearModal; all app-runtime coupling (selected, sessionIndex, polling, recovery UI, toasts, auth loss, modal coordination, DOM factory) is injected. No silent fallback: missing required deps throw TypeError. app.js keeps DOM creation for the queue nodes, thin delegating wrappers (syncQueueSubmitState/enqueueComposerText/refreshQueueViewer/showQueueViewer/hideQueueViewer), and the dispose hook (`if (queueController) queueController.dispose()`). updateQueueBadge stays in app.js but calls the refresh wrapper when the viewer is open. Recovery panel `Review queue` button still opens the queue via the wrapper. The script-load invariant `app_launch < app_display < app_new_session < app_dom` is untouched. Tests: new `test_frontend_queue_module_source.py` (VM-executed behavior tests across all queue contracts), plus test_queue_button_source / test_auth_cleanup_source / test_overlay_accessibility_source / test_chat_scrollback_source / test_static_assets updated to assert controller-owned behavior instead of app.js queue internals. Remaining app.js concentrations: chat search/navigation orchestration, recovery panel view factories, unattended, diagnostics, voice.
- Next architecture target from read-only scout: continue with the remaining app.js concentrations (chat search/navigation, recovery panel view factories) now that queue is controller-owned.


## Queue extraction state
- Queue orchestration now has a controller authority (`CodoxearQueue`) after `52f3021`. `app.js` no longer owns queue timers/locks/drafts/viewer item state; it owns DOM construction/wrappers/recovery panel. Browser evidence on 19090 proved the module load and modal path with a fake discovered session; Docker and clean browser evidence after commit confirm the new asset is registered.
- Remaining app-shell concentrations after New Session + Queue: chat search/navigation orchestration, recovery panel rendering, unattended menu, diagnostics/details, voice/notifications. The prior scout ranked chat search and voice as higher-risk; diagnostics/details is lower payoff; unattended is coherent but touches recently fixed failed-launch controls.


## Post-queue review judgment
Clean-room critic 4aad1a07 accepted the load-error/composer/queue tranche with no blockers. The strongest remaining mechanism is load-error visibility after partial root mount: `__codoxearLoadError` records the error, but the visible fallback only renders when `#root` is empty. A failure after app shell mount but before bootstrap completion could leave a broken skeleton. Next hardening should add an explicit successful-bootstrap marker and render the fallback when an error is recorded before that marker, even if root has partial content.


## Sentinel hardening state
- Partial-root load failures now surface visibly. The sentinel has an explicit successful-bootstrap marker; login/app render mark only after a valid synchronous UI exists. If a load error is recorded before that marker, the fallback clears any partial skeleton and shows the error panel. Browser evidence on 19093 proves normal load, partial-root failure, and post-bootstrap error separation.
- The source-test failure after this change was a test-locator artifact: broad `index("app.js")` matched a comment rather than the script tag. Tests now pin exact asset-version script markers.

## Diagnostics/details extraction state
- Details/diagnostics modal orchestration now has a controller authority (`CodoxearDiagnostics`, `codoxear/static/app_diagnostics.js`) loaded after `app_session_helpers.js`/`app_modal.js`/`app_queue.js` and before `app.js`. `createDiagnosticsController(options)` owns all diag state (`diagReturnFocusEl`, `diagCopyText`, `diagNewLikeSession`), the show/hide modal+focus behavior, the New-like-this / Copy details click handlers, and the three rendering paths: failed-launch local recovery rows (no `/diagnostics` call), live session fetch with stale-response guard (`getSelected() !== sid` short-circuits in both success and catch), and error path (clears copy/new-like, disables buttons, writes `error: ...`). Module contract: IIFE, `Object.freeze({ createDiagnosticsController })`, TypeError on missing function/string deps, Error on missing module globals. app.js keeps DOM construction for all diag nodes, the `diagBtn` opener with `diagBtn.disabled = !selected`, the `diagCloseBtn`/`diagBackdrop` wiring, thin `showDiagViewer(opts)`/`hideDiagViewer(opts)` wrappers that delegate, and the dispose hook. Pure helpers (`diagnosticsProviderDisplay`/`diagnosticsCopyText` via `sessionAgentBackend`) and recovery text/preset factories (`recoveryDetailsText`, `launchPresetFromSessionInfo`) stay in app.js and are injected. No duplicate rendering authority remains in app.js (the old ~170-line inline `showDiagViewer` body is gone).
- Tests: new `test_frontend_diagnostics_module_source.py` (20 VM-executed behavior tests across all 8 contract areas: frozen export, missing-dep TypeErrors, failed-launch local path, live `/diagnostics` path with Provider/Model/Reasoning/Context/UI rows + copy text + new-like preset, stale-response ignore for both success and error, error path disabling buttons, copy button exact `Copied details` toast + copy-failed path, New-like click hide-without-focus-restore + open New Session, show/hide modal+backdrop+focus, dispose). `test_diagnostics_source.py` reduced from app.js inline-body assertions to app.js-integration + module-VM-locatable checks (helper tests kept executable). `test_overlay_accessibility_source.py` updated: Help show/hide stays asserted in app.js; Diag show/hide now asserted in the module. `test_new_session_model_options_source.py` New-like assertions retargeted to the module. `test_static_assets.py` registers `app_diagnostics.js` across manifest/ordering/CSP/wheel/version-change/read-static-bytes checks.
- Validation/evidence after commit `d22b686`: local syntax checks passed; targeted diagnostics/static/frontend suites passed (`52 passed`, `22 passed`); full local pytest passed (`1405 passed, 132 subtests`); Docker test on sandbox 19095 passed (`1404 passed, 1 skipped, 132 subtests`). Browser evidence on sandbox 19094 proved the user-visible Details paths: failed-launch Details rendered local rows with no load error and enabled Copy/New-like (d41); fake live session Details rendered fetched diagnostics rows including Provider/Model/Reasoning/Context/UI and enabled actions (d42); clipboard denial surfaced as an explicit toast; New-like opened New Session with copied cwd/model/reasoning preset (d43).
- Acceptance judgment: clean-room critic retry `1750e854` accepted `d22b686` with no blockers after comparing the old inline app.js Details implementation against the new controller. The accepted mechanism is behavior-preserving extraction plus single ownership: stale guards, failed-launch no-API rows, live `/diagnostics` rows/new-like preset, copy/new-like toasts, and focus/backdrop/Escape behavior are preserved, while `CodoxearDiagnostics` owns diag state/rendering/actions and app.js only delegates. Non-blocking risk: one VM test contains unused placeholder locals, but companion executable assertions cover the behavior.
- Remaining app-shell concentrations after New Session + Queue + Diagnostics: chat search/navigation orchestration, recovery panel rendering, unattended menu, voice/notifications.

## Recovery panel extraction state
- Recovery-panel rendering/actions/focus now has a controller authority (`CodoxearRecovery`, `codoxear/static/app_recovery.js`) loaded after `app_session_helpers.js`/`app_diagnostics.js` and before `app.js` (and before the queue controller instantiation in app.js). `createRecoveryPanelController(options)` owns `pendingRecoveryFocusDescriptor`, the internal `recoverySessionInfo`/`focusedRecoveryActionDescriptor`/`focusRecoveryAction`/`focusRecoveryFallback`/`focusFallbackCandidate` helpers, and the full Launch failed / Recovery needed panel render (row removal, title/list/preview/actions, insert-before-typing-anchor, focus preservation across rerender via text+title match then fallback to first `.recovery-panel .icon-btn` then queue button). Module contract: IIFE, `Object.freeze({ createRecoveryPanelController })`, TypeError on missing function deps, Error on missing `CodoxearSessionHelpers` global. Returns `{ render, renderRecoveryPanelIfNeeded, focusFallbackCandidate, dispose }`.
- app.js keeps the pure helpers (`recoveryPromptPreview`, `launchPresetFromSessionInfo`, `recoveryDetailsText`, `dismissFailedLaunchRecord`, `clearCommitUnknownSend`, `redactedLaunchErrorText`) so diagnostics and recovery share a single source of truth, keeps `syncRecoveryUiForSession` (status/button projection) which delegates panel rendering to the controller, keeps a thin `renderRecoveryPanelIfNeeded` wrapper, and instantiates `recoveryController` before the queue controller so the queue's `recoveryPanelFocusFallback` dep routes through `recoveryController.focusFallbackCandidate()` instead of app.js querying `.recovery-panel .icon-btn` directly. Dispose hook added.
- Tests: new `test_frontend_recovery_module_source.py` (20 VM-executed behavior tests: frozen export, missing-dep TypeErrors, no-state removes rows/returns false, failed-launch render+insert-before-anchor, orphan/queue/commit-unknown render, Review queue/Clear unknown/New-like/Dismiss/Copy details click contracts incl. no-preset and copy-failed paths, focus preservation across rerender, queue-button fallback when no panel remains, focusFallbackCandidate null/non-null, dispose, index load order, app.js delegation). `test_chat_scrollback_source.py` reduced: the brittle app.js recovery-panel source assertions retargeted to module assertions + app.js integration checks; `eval_launch_recovery_helpers` now exercises the pure helpers that remain in app.js (`recoverySessionInfo` moved out, so `hasInfo` dropped). `test_static_assets.py` registers `app_recovery.js` across manifest/ordering/CSP/wheel/version-change/read-static-bytes checks.
- Validation/evidence after commit `4b9a8df`: local syntax checks passed; targeted frontend+static+queue+diagnostics suites passed (`103 passed`, `10 passed`); full local pytest passed (`1425 passed, 132 subtests`); Docker test on sandbox 19097 passed (`1424 passed, 1 skipped, 132 subtests`). Browser evidence on sandbox 19096 proved the user-visible recovery paths: failed-launch recovery panel rendered through `CodoxearRecovery` with no load error and actions New-like/Dismiss/Copy (d44); orphan queue-recovery row projected from real persisted `session_queues.json` rendered Recovery needed panel with Review queue/Copy details and correct structural send/composer blocking (d45); Review queue opened the queue modal with focus on close (d46); Copy details surfaced clipboard denial explicitly; New-like opened New Session with copied cwd/model/reasoning preset (d47).
- Acceptance judgment: clean-room critic `fcdc3b58` accepted `4b9a8df` with no blockers after line-by-line comparison to the old app.js recovery block and mutation-confirmation of the tightened VM tests. The accepted mechanism is behavior-preserving extraction plus single ownership: recovery predicate, row sweep, Launch failed / Recovery needed panel content, all five actions, insertion before typing anchor, focus preservation/fallback, fail-loud deps, and queue fallback wiring are preserved, while `CodoxearRecovery` owns recovery-panel state/rendering/actions and app.js only delegates. Non-blocking risks: the `chatInner.querySelector` fallback assumes the recovery panel remains singleton inside `chatInner`; dispose does not cancel an already scheduled rAF but teardown makes this harmless.
- Remaining app-shell concentrations after New Session + Queue + Diagnostics + Recovery: chat search/navigation orchestration, unattended menu, voice/notifications.

## Unattended mode extraction state
- Unattended menu state/load/save/input/focus now has a controller authority (`CodoxearUnattended`, `codoxear/static/app_unattended.js`) loaded after `app_recovery.js` and before `app.js`. `createUnattendedController(options)` owns `unattendedMenuOpen/Token/SessionId/ReturnFocusEl`, `unattendedCfg`, `unattendedNumberDraft/Dirty`, the per-session `unattendedSaveTimers/InFlight/Pending` maps, the parse/sync/finalize/restore number-draft helpers, `loadUnattendedCfgForSelected` (with stale open-token/session/selected guards + validation), `validateUnattendedPayload`, `unattendedSaveSnapshot`/`applySavedUnattendedCfg`/`flushUnattendedSave`/`scheduleUnattendedSave` (450ms per-session debounce, snapshot coercion/merge, in-flight blocking, pending drain, 401 auth-loss), the `projectButtonState` unattended button+cfg+input projection, menu show/hide/toggle positioning + focus (`restoreModalFocus`), the `unattendedBtn.onclick`/`unattendedMenu.onclick`/control `onchange/oninput/onblur` handlers, and the document Escape/click + window resize listeners via injected `addAppEvent`/`documentTarget`/`windowTarget`. Module contract: IIFE, `Object.freeze({ createUnattendedController })`, TypeError on missing function/node deps, Error on missing `CodoxearSessionHelpers`/`CodoxearModal` globals. Returns `{ syncButtonState, show, hide, toggle, isOpen, menuSessionId, dispose }`.
- app.js keeps DOM construction for `unattendedBtn`/`unattendedMenu`/all controls, `updateUnattendedBtnState()` as app-shell projection (title-edit/attach/file/copy/chatSearch/context-bar/nav/queue/send/diag) that delegates the unattended-specific projection to `unattendedController.syncButtonState()`, thin wrappers `hideUnattendedMenu/showUnattendedMenu/toggleUnattendedMenu`, and the dispose hook. `requestShellProjection: updateUnattendedBtnState` is injected so input handlers re-run the full shell projection exactly as before extraction; `closeTransientOverlays`/`clearSelectedSessionAfterRemoval`/`selectSession` callers delegate via `unattendedController.isOpen()`/`menuSessionId()`. The hide-when-session-changes guard moved inside `syncButtonState` (runs earlier in the projection order, behavior-preserving since non-unattended projections don't depend on menu state).
- Tests: new `test_frontend_unattended_module_source.py` (25 VM-executed behavior tests across all 13 contract areas: frozen export + missing-dep TypeErrors + fail-loud module load, button projection no-selected/failed-launch/active, show blocked failed-launch exact toast + no-selection no-op, show opens+disables+positions+fetches+validates+enables+focuses+token/session guards, controls-disabled-during-load, invalid-response rejection, stale-load ignored when selected/menu changes, load-error toast+hide+focus-restore, number-draft preservation+invalid-blur restore, remaining<=0 disables enabled+save snapshot, debounce/merge/per-session/in-flight+pending-drain, 401 auth-loss vs non-401 error toast, applySavedCfg updates fields with selected/menu guards, Escape/outside-click/resize hide+focus, dispose clears timers/pending/in-flight+invalidates menu, index load order, app.js delegation). `test_unattended_mode_source.py`/`test_unattended_input_source.py` retargeted from app.js internals to module-source + app.js-delegation assertions (backend/readme assertions unchanged). `test_static_assets.py` registers `app_unattended.js` across manifest/ordering/CSP/wheel/version-change/read-static-bytes. `test_auth_cleanup_source.py`/`test_overlay_accessibility_source.py`/`test_chat_scrollback_source.py`/`test_voice_push_source.py` updated for the new delegation/boundary markers (VM contexts now inject an `unattendedController` mock).
- Validation/evidence after functional commit `bd40386`: node --check both files passed; targeted unattended/static/session-adjacent suite passed (`107 passed`); full local pytest passed (`1452 passed, 132 subtests`); Docker test on sandbox 19098 passed (`1451 passed, 1 skipped, 132 subtests`). Browser evidence on sandbox 19099 proved the user-visible paths: clean load has `CodoxearUnattended` and no load error; failed-launch row disables the unattended button with exact label `Failed launch has no unattended mode` (d48); a selectable fake control-socket session opens the popover, saves enabled/cooldown/request through the real `/unattended` API, keeps DOM values synchronized, and restores focus on Escape (d49); mobile 390x844 popover fits within viewport with saved config and no load error (m09).
- Acceptance judgment: clean-room critic `130bba9c` accepted `bd40386` with no blockers after line-by-line comparison to the old app.js block. The accepted mechanism is behavior-preserving extraction plus single ownership: failed-launch blocking, no-selection/active button projection, popover positioning, stale-load guards, 450ms per-session save debounce/merge/in-flight/pending drain, 401/auth-loss and non-401 toast behavior, remaining<=0 disabling, dirty number drafts, Escape/outside/resize hide, focus restore, and dispose invalidation are preserved while `CodoxearUnattended` owns unattended state/actions and app.js delegates. Non-blocking risks: optional defaulting of injected browser globals could be made stricter; control nodes are captured once, which relies on the existing one-time shell DOM construction; some source-pattern tests remain substring-brittle but VM tests carry behavior.
- Remaining app-shell concentrations after New Session + Queue + Diagnostics + Recovery + Unattended: chat search/navigation orchestration, voice/notifications.

## Chat navigation extraction state
- Loaded-chat navigation rail and document shortcut orchestration now has a proposed controller authority (`CodoxearChatNavigation`, `codoxear/static/app_chat_navigation.js`) loaded after `app_unattended.js` and before `app.js`. `createChatNavigationController(options)` owns prev/next button projection, loaded user-message jumping, all-loaded-message copy-row jumping, prev/next click handlers, shortcut blocking, and document shortcuts for `/`, `Alt+Shift+↑/↓`, and `Alt+↑/↓`. app.js keeps DOM construction, chat search internals/older-history search, row pulse/message-copy active-row helpers, and thin wrappers.
- Validation/evidence after functional commit `9b2d5fc`: node --check both files passed; targeted+adjacent frontend suite passed (`152 passed`); full local pytest passed (`1485 passed, 132 subtests`); Docker test on sandbox 19100 passed (`1484 passed, 1 skipped, 132 subtests`). Browser evidence on sandbox 19101 proved module load with no load error, enabled nav rail over a fake transcript with 3 user rows, Next-user button pulse (d50), `/` shortcut opening search while preserving search internals (`1/2 loaded · 2 all`), `Alt+Shift+ArrowDown` all-message shortcut pulse (d51), and mobile nav rail visibility/enabled state (m10).
- Acceptance judgment: clean-room critic `25d50fb3` accepted `9b2d5fc` with no blockers after comparing the extracted controller to the old app.js bodies. The accepted mechanism is behavior-preserving extraction plus single ownership: button enabled/disabled projection, user-message and all-loaded-message navigation toasts, scroll threshold/behavior, row pulsing, prev/next handlers, `/` search shortcut, Alt-arrow shortcuts, shortcut blocking predicates, cleanup via `addAppEvent`, and fail-loud load order are preserved while `CodoxearChatNavigation` owns navigation state/actions and app.js keeps search internals and wrappers. Non-blocking risks: optional `documentTarget`/`isSidebarOpen` defaults could be made stricter; `jumpToLoadedUserMessage`/`jumpToLoadedMessage` app.js wrappers are now unreferenced but harmless; keydown listener cleanup still relies on app-wide `appEventCleanups` as sibling controllers do.
- Remaining app-shell concentrations after ChatNavigation: deeper chat search/older-history search internals and voice/notifications.

## Chat search extraction state
- Loaded-chat search and older-history search orchestration now has a controller authority (`CodoxearChatSearch`, `codoxear/static/app_chat_search.js`) loaded after `app_chat_navigation.js` and before `app.js`. `createChatSearchController(options)` owns the loaded-match runtime, all-transcript count scheduling/runtime, search status/all-hint projection, focus/marking, open/close, prev/next step semantics, page-loop older loading, nearest-older search-window lookup, cursor-window `/messages/history` loading, search-bar event handlers, and dispose cleanup.
- app.js keeps DOM construction for the search bar/controls, row text/mark helpers, row pulse behavior, transcript detached-window rendering, older-load runtime authority, auth-loss/session-refresh responses, and thin wrappers (`openChatSearch`, `closeChatSearch`, `refreshLoadedChatSearch`, `stepChatSearch`) used by app-shell and chat-navigation code. No duplicate search runtime authority remains in app.js.
- Validation/evidence after functional commit `b4eae26`: node checks passed; targeted/adjacent suite passed (`221 passed` before cosmetic cleanup, `76 passed` after duplicate-comment cleanup); full local pytest passed (`1527 passed, 132 subtests`); Docker test on sandbox 19102 passed (`1526 passed, 1 skipped, 132 subtests`). Browser evidence on sandbox 19103 proved fail-loud load order (`bootstrapped=true`, `loadError=null`, `CodoxearChatSearch` present, scripts `app_chat_navigation.js -> app_chat_search.js -> app.js`), loaded-tail search (`loadedneedle`, `1/1 loaded · 1 all`, row `chat-search-hit chat-search-current`), older-only all-count hint (`deepneedle`, `0/0 loaded · 1 all`, all-hint, no loaded hits), cursor-window loading via search/history route (`Loaded transcript match`, older row focused, `1/1 loaded · 1 all`), and mobile 390x844 retained the open search bar and focused older row. Screenshots d52-d54 and m11; raw observations in OPS.
- Acceptance judgment: clean-room critic `b657fd99` accepted the extraction with no blockers after reviewing the actual diff. The accepted mechanism is behavior-preserving extraction plus single ownership: all old app.js search internals moved into `CodoxearChatSearch`, missing deps throw synchronously, static order satisfies the new hard dependency, app.js only delegates, and VM tests execute the real transcript search runtimes. The only critic note was a duplicated comment, removed before the functional commit.
- Remaining app-shell concentrations after ChatNavigation + ChatSearch: voice/notifications and any small residual app-shell projection code. The transcript/search/navigation cluster is now controller-owned at the orchestration level; app.js still appropriately owns transcript rendering authority and shared row helpers.


## Voice/settings/notifications extraction state
- Controller authority: `CodoxearVoice` in `codoxear/static/app_voice.js`, loaded after `app_voice_helpers.js` and before `app.js`. The controller owns voice settings load/save/projection, API-key placeholder/clear handling, announcement and notification toggles, notification feed state/projection, live-audio/HLS event wiring, settings dialog show/hide/focus state, and cleanup. app.js keeps DOM construction and thin wrappers (`loadVoiceSettings`, `syncNotificationState`, `pollNotificationFeed`, `resumeAnnouncementRuntime`, `showVoiceSettingsDialog`, `hideVoiceSettingsDialog`, `voiceAnnouncementsEnabled`, `notificationsEnabledLocally`).
- Product evidence supports the mechanism: browser sandbox 19107 loaded with `CodoxearVoice` present and no load error; the side settings control and dialog now say `Voice settings`; missing credentials reopen the settings dialog with an explicit credential-required status; dispatching an actual `error` event on the hidden live audio element makes the announcement button active+error, updates title/ARIA to `Announcements on (audio error)`, and visibly renders red border/background/text on desktop and mobile.
- A real browser observation falsified the first CSS fix: `#announceBtn.error.active` had red border/background but blue text because a later `#announceBtn.active, #notificationBtn.active` rule supplied `color: var(--accent)`. The mechanism is cascade order plus missing explicit color in the more specific combined rule. The final fix sets `color: #b91c1c` on `#announceBtn.error.active` and tests that rule against the later active rule.
- Acceptance judgment: clean-room critic `7566cbdb` accepted the extraction with no blockers; functional commit `f42d715` and evidence commit `c3693df` landed. Current validation: node checks for `app_voice.js` and `app.js`; targeted voice/static/auth/overlay suite `61 passed`; full local `1542 passed, 132 subtests`; Docker unit on 19106 `1541 passed, 1 skipped, 132 subtests`; Docker smoke on 19108 pre-login 401/post-login 200; browser sandbox 19107 proved module load, Voice settings dialog, credential-required status, and desktop/mobile visible red active+error state.
- Accepted behavior deltas: dead desktop-notification resolver helpers were removed; push-subscribe base64 conversion now passes the required `atob` helper argument; notification-feed 401 now triggers `handleAppAuthLoss()` instead of silent swallow. These are judged improvements/non-regressions by the critic.


## Release-candidate roadmap correction
- The correct project state after `c3693df` is feature-complete release candidate awaiting whole-product certification, not open-ended refactor recovery. The browser controllers named in the original architecture tranche are extracted through Voice/settings/notifications; further extraction is possible only as a mechanism-specific intervention.
- The discarded sidebar/swipe extraction is negative evidence: a plausible source-level issue is insufficient to justify a broad module split without browser/product confirmation. If sidebar swipe behavior is later proven false to user reality, fix the specific mechanism first and extract only if needed for single authority/testability.
- The roadmap now has ordered decisions: certify current composition, determine backend parity scope, fix evidence-backed failures, close bounded non-product debt if it still matters, then clean-room review and promotion proposal.

## Whole-product certification anomaly: Pi pre-log failure visibility
- Certification of the composed release candidate exposed one real product bug and one environment boundary. The unhealthy Docker Pi backend exited before creating any session log because the container's lazily-installed Pi extension set fails on an `lsp-pi` package-exports mismatch, and `deepseek` lacks an API key in the throwaway home. That explains the backend death, but not the silent browser empty state.
- The Codoxear bug was the broker's bound-log invariant: Pi launches reserve a future `--session <path>.jsonl`, but broker startup treated that declared path as `st.log_path` before the file existed. Because the web-owned pre-log-exit guard only checked `st2.log_path is None`, no `agent_exit_before_log_bind` ledger record was written; after sidecar unlink, session discovery had nothing to project. The UI therefore returned to empty state instead of showing recovery. OPS: 2026-07-04T03:48:00Z.
- The fix makes `st.log_path` mean an observed/bound log file, matching `session_discovery.py` semantics. Declared-but-missing Pi session paths remain separate until the watcher observes a file; the final guard treats missing log paths as unbound. Product verification in the same unhealthy Pi sandbox now projects a failed launch row with `launch_stage=agent_exit_before_log_bind` and renders the recovery panel rather than `sessions: []`. OPS: 2026-07-04T03:59:00Z.
- Current release implication: failure visibility for pre-log web-owned Pi exits is supported as fixed by unit and browser/API evidence. Full release acceptance still requires the planned clean-room review. Real Pi success certification is still not complete in container `19110`; it requires either repaired Pi extensions plus valid provider credentials or a different healthy provider/config. Backend parity remains open.

## Rejected certification workaround: Pi no-default-extensions UI
- A proposed Pi-only New Session checkbox for passing `-ne` was reverted. The option solved a sandbox certification problem, not a proven user workflow. Exposing internal launch flags because the test container has a broken Pi extension set is product-surface pollution.
- The review failure mechanism was contract contamination: the critic was asked whether the checkbox preserved launch semantics, reset state, and had accurate mechanism text. It was not asked whether the option should exist in the product. A review constrained to an invalid premise can only audit implementation quality inside that premise.
- Correct boundary: if Docker certification is blocked by broken Pi extensions or missing credentials, repair the certification environment, launch a terminal/API session only as diagnostic evidence, or state a release boundary. Do not convert the workaround into UI.

## Product reanchor and Pi success state
- Product invariant: Codoxear is a faithful browser projection/control channel for local CLI-agent sessions whose truth lives on disk. The user promise is "trust the phone to be the terminal": every real session state must be visible/controllable without silent disappearance, and every visible control must map to a user workflow.
- The invalid `-ne` UI episode is absorbed as a review-framing failure. Future reviews must ask whether a product surface should exist before auditing implementation containment.
- Ordinary browser-created Pi success is now proven after sandbox-only environment repair: New Session -> Pi -> `openai-codex/gpt-5.4-mini` without `-ne` created `broker-30465`, bound a real log, and rendered assistant `CERT-OK`. The Pi extension repair is an environment condition, not a Codoxear product change.
- Remaining blocking evidence before promotion: composed-controller flows on the live Pi page and server-restart continuity; backend parity must be proven or bounded per backend.

## Composed Pi certification and backend parity state
- Pi is now certified as a composed browser product path at current HEAD after sandbox-only environment repair: ordinary New Session, real response, search/navigation, queue/drain, interrupt, live diagnostics, file viewer, unattended/voice projections, and mobile all worked on `broker-30465`. No blockers or impairing issues were found. The observed git, Monaco, and clipboard issues are environment boundaries with explicit UI behavior rather than silent product failure.
- Backend parity: Codex has fresh real CLI -> log -> parser evidence, but browser-level Codex proof is still pending. Claude Code has real-log parser evidence and launch/log wiring, but fresh end-to-end is blocked by an external gateway 503; this is currently a release boundary rather than a Codoxear bug.
- Remaining promotion gates are concrete: canonical Docker test/smoke at current HEAD, browser-Codex smoke or explicit boundary, server-restart continuity from the browser, then clean-room release review.

## Validation and backend-boundary state
- Canonical validation is green for the current functional tree: full local pytest and Docker test/smoke passed after the broker pre-log fix and the no-extension UI reverts.
- Browser-Codex does not currently support an end-to-end claim in the certification sandbox. The browser-created Codex broker reached pending-bind but never bound a rollout log because the Codex TUI stalled on expired MCP auth; Codoxear projected this honestly (`broker_busy=true`, send gated). This is an upstream-auth boundary, not a product projection defect.
- Codex has fresh non-browser CLI/log/parser evidence; Claude Code has real-log parser evidence but fresh CLI is blocked by gateway 503. Backend parity claim must be scoped accordingly unless browser/CLI retries later succeed.

## Restart/rediscovery continuity state
- True server restart cannot be performed safely in the current certification container because the server is PID 1 and owns the process tree containing live brokers. Killing it would destroy the sessions whose continuity is being tested.
- This is a harness boundary, not a Codoxear product defect. The next best discriminator is a fresh second server process against the same app dir: if it independently rediscovers `broker-30465` and its transcript, the disk-source-of-truth rediscovery mechanism is supported even though destructive restart remains unproven in this harness.

## Fresh-server rediscovery proof
- The disk-source-of-truth invariant is supported by a fresh-process proof: a second independent server sharing the same app dir rediscovered `broker-30465` and reconstructed the same transcript state and `CERT-OK` content as the original server. This tests the product mechanism that matters for restart continuity: no browser/session truth is trapped only in server memory.
- Literal restart of the PID1 server remains a harness boundary because killing it would kill brokers in this container. A production-style supervisor/host restart would be the exact final proof, but current evidence supports the Codoxear mechanism rather than exposing a product defect.

## Codex pre-log busy deadlock
- Browser-Codex is blocked by both an environment trigger and a Codoxear readiness bug. The environment lacks working Codex credentials, causing MCP startup failures/noise before a rollout log exists. Codoxear then interprets the pre-log `esc to interrupt` hint as busy and has no pre-log idle-clearing path, so the first browser send is rejected forever as `session is busy; wait before sending`.
- This is a product bug because a backend that has not produced a transcript yet can become permanently unsendable based only on pre-log TUI text. The fix should preserve fail-loud backend errors while preventing pre-log startup noise from deadlocking first input. No user-facing UI or debug option belongs here.

## Codex pre-log busy fix state
- The Codex pre-log busy deadlock is fixed at the readiness-synthesis layer. Broker-internal PTY busy remains truthfully recorded, but pre-log broker busy no longer blocks the first browser input because no transcript/log authority exists yet and no idle-clearing path can run before log bind.
- Safety boundary after the first input is preserved by the confirmed-send boundary: once the browser attempts a confirmed send, follow-up input is blocked until the log appears/advances or the send becomes an explicit unknown-send recovery state. Post-log busy turns still gate through `log_idle=False`.
- Remaining Codex release boundary is environment authentication/inference, not this readiness deadlock. Attachment injection shares the pre-log readiness relaxation; keep this as a policy edge to revisit only if browser evidence shows user-visible misdelivery.

## Post-fix backend boundary state
- Codex pre-log readiness is no longer a product blocker. A fresh fixed server reproduced `broker_busy=true` before log bind and accepted the first browser/API send with HTTP 200; the broker then bound a rollout log and recorded the user message. The previous `session is busy; wait before sending` deadlock is falsified at current HEAD.
- Codex browser inference remains bounded by credentials/provider state in the certification container: the degraded backend produced `task_complete` without assistant text. The supported claim is readiness/control repair, not Codex parity.
- Attachment injection sharing the pre-log readiness relaxation is a non-blocking edge: it uses the same bracketed-paste PTY path as first text send and enters a one-attachment pending state until explicit send. No separate code change is justified absent browser evidence of misdelivery.

## Corrected organizing frame
- The deliverable is continued whole-product iteration, not a promotion proposal. The product category is a multi-backend local-agent browser controller; certifying Pi alone does not settle the promise exposed by Codex and Claude tabs.
- Backend boundaries are product questions when they affect what the user sees. A failed credential/gateway/empty-turn condition cannot live only in OPS notes if the browser projects it as silence or ordinary idle.
- Next uncertainty: after Codex first-send readiness was fixed, degraded Codex produced a bound log with a user message and `task_complete` carrying no assistant text. The decisive question is whether Codoxear surfaces that backend truth as an explicit no-response/error/boundary state or leaves the user with a silent missing answer.

## Selectable backend promise
- A selectable backend is a user-facing promise. Backend availability failures are external causes, but Codoxear still owns the projection: a user prompt followed by a completed turn with no assistant/error output must become explicit no-response/failure state in the transcript/API, not quiet idle.
- Therefore Codex credential failure cannot be accepted merely as a release boundary while the UI shows a normal empty turn. The next product fix is to normalize empty completed turns into truthful visible events without inventing assistant content.


## Surface-map correction and binary busy/idle authority
- The surface certification map is an internal planning tool, not product vocabulary. Files, repository status, and mobile companion usability are first-class product surfaces because they are core user situations in Codoxear: a remote local-agent UI must let the user inspect/edit files, understand repository state, and control sessions from a phone without ambiguity.
- Busy/idle is binary. The product risk is not missing intermediate states or colors; it is duplicated authority for the same boolean. Broker turn state, log idle heuristics, runtime readiness synthesis, `/api/sessions` flags, composer disabled state, queue/interrupt controls, sidebar blue/gray indicator, chat spinner/animation, mobile status text, and unattended sweep eligibility must all derive from the same busy boolean.
- Answer, backend error, no-response completion, and interruption are transcript messages/recovery messages. They are not additional busy states and should not create extra colors, legends, or mappings. Once the backend is not working, the session is idle for any reason. The next audit must map derivations and test corner cases that previously generated failures: pre-log first send, backend API error message, no-answer message, interruption, queue while busy/drain, backend exit before log bind, pagination/live polling, stale sidecars, and rediscovery.

## Binary busy/idle audit result
- The current architecture mostly preserves the corrected model: `busy` is a single boolean computed by `session_runtime.resolve_runtime_status(...)`; answer/error/no-response/interruption/failure are transcript or recovery messages, not session-state categories.
- The only real busy projection seam found is stale `interrupted_idle` in the listing path. If a session was interrupted and then a same-log turn resumes outside the selected live-poll path, `/api/sessions` may use the stale interrupted-idle override and show idle while the log is non-idle. The right fix is to invalidate/refresh that boolean input when newer activity appears, not to add state vocabulary.
- A dead `.stateDot.failed` CSS rule conflicts with the binary model even though app.js no longer emits it. Removing it prevents future reintroduction of failed-as-color semantics; failed launches remain ordinary idle/unavailable rows with a failed badge/recovery message.

## Current binary-state/message projection state
- Codex no-response is now a transcript-message problem, not a session-state problem. If a Codex user turn closes with no visible assistant transcript message, Codoxear emits an explicit no-response message. If Codex provides assistant text via `response_item`, `event_msg.agent_message`, or `last_agent_message` on the close row, that text renders as an assistant message and no synthetic no-response appears.
- The binary busy/idle projection is cleaner: the dead failed-dot color is removed, and stale `interrupted_idle` is invalidated by post-interrupt resumed activity. Immediate interrupted-idle behavior remains available for the case it was designed for: the backend has stopped working but the log is non-final because the interrupted turn never wrote a clean close.
- Remaining claims still require browser/Docker re-verification before product acceptance: these fixes are supported by targeted unit/source tests and mechanism review, not yet by a fresh browser certification run.

## Verification boundary correction: host throwaway HOME is not isolation
- The tmux-loss incident falsified the practice of treating host-side throwaway `HOME` as a safe backend/session repro boundary. `HOME` redirects config/log paths only; it does not isolate the host process table, tmux socket, signals, `/tmp`, or systemd. A host command such as `pkill -f codoxear.broker` can kill live Codoxear brokers even when all runtime files point at a throwaway app dir.
- Correct mechanism: broker/server/session/tmux verification belongs in Docker. If Docker is unavailable, the result is an unverified boundary, not a reason to substitute a host throwaway-HOME repro. Agent-run contracts must forbid `pkill -f`, `killall`, and broad `pgrep | xargs kill`; cleanup is exact-PID for a process just started by the agent or container-scoped.
- Orchestration implication: main-agent convenience is not a product mechanism. Delegation contracts must state the isolation boundary explicitly, and reviewers must treat host throwaway-HOME backend/session repros as invalid evidence.

## Post-fix evidence: CC binding and file editing fail loud
- The CC cwd-mismatch fallback is now supported by unit tests and Docker/browser evidence. In Docker, a fake Claude binary wrote a fresh closed log whose recorded cwd differed from the broker cwd; Codoxear bound the log, `/messages/tail` reported `transcript_state=bound`, and the browser rendered the assistant text. This resolves the prior `pending_bind/log_path=null despite on-disk answer` product bug for the tested mechanism.
- The file-editing Monaco-unavailable Edit path now fails loud. In Docker where `/monaco/vs/loader.js` returns 404, editable text files render the plain fallback and the Edit button remains event-receiving with `aria-disabled=true`; clicking it surfaces `Editing is unavailable because the code editor failed to load. Read-only preview remains available.` in status/toast instead of silently disabling/no-oping.
- Regression evidence covers the affected user surfaces: text file list/search/read, binary download-only preview message, git changed_files/diff, and mobile 390x844 file-viewer readability. Current validation after the functional fixes: full local `1627 passed, 132 subtests`; Docker test `1626 passed, 1 skipped, 132 subtests`; Docker smoke 401/200.
- Residual boundary: fake-Claude proof validates binding/transcript projection, not real Claude inference quality or CC idle semantics. Real-backend parity still depends on provider/credential health, but the projection/control mechanism for this binding failure is now supported.

## File D2: non-UTF filename list/search crash resolved
- The D2 failure mechanism was a serialization boundary, not file traversal itself: `os.walk` produced surrogateescape path strings for non-UTF-8 filenames, and Codoxear sent those raw surrogates into JSON bodies that are encoded as UTF-8. A single such filename made the whole `file/list` or walk-mode `file/search` endpoint fail with a raw codec error, hiding even normal neighboring files.
- The committed fix projects walk/list display paths through `path_json_text`, the same surrogate-safe display convention used by the git path layer. The supported claim is now: non-UTF-8 filenames inside the session cwd no longer crash list/search response serialization; they appear as display paths like `bad\xffname.bin`, while normal UTF-8 paths remain unchanged.
- This does not claim reversible opening/editing of those non-UTF walk/list results. That was not a working pre-existing contract; it requires additive `api_path`/`path_token` support for plain file results and endpoints. The current fix is therefore a strict improvement over total endpoint failure and leaves openability as a separate file-flow enhancement.
- Docker/browser verification now supports the user-facing claim: in an isolated Docker deployment, API list/search returned HTTP 200 for raw-byte filenames and the browser file picker rendered `bad\\xffname.bin` instead of the previous raw codec error.

## File D3: mobile file-viewer toolbar touch targets fixed
- The D3 mechanism was CSS cascade, not layout overflow: mobile/global `.icon-btn` sizing made file-viewer toolbar buttons 34-40px tall/wide, and `#fileEditBtn { min-width: 38px; }` specifically kept Edit below 44px even after a broad mobile rule. The fix uses mobile-scoped `min-width/min-height: 44px` for file-viewer toolbar icons and an explicit mobile override for `#fileEditBtn`.
- Browser evidence at 390x844 now measures the visible toolbar controls at 44x44 with no horizontal overflow. The dpad `.fileTouchBtn` controls remain a separate fixed-grid touch target issue.

## File D4: non-UTF filename openability is partially supported, not closed
- The plain walk-mode token channel is supported by Docker API evidence: a raw-byte filename rendered as `bad\\xffname.txt` carried an `api_path` token, and read/download/write with `path_token` addressed the real bytes without JSON codec errors. This proves the reversible operation mechanism for one plain-file path, not the full file-surface claim.
- The full user-facing invariant remains open: every visible file choice must carry an operational identity through search, open, download, save, git discovery, and recent/history replay. Source review now supports the main identity repair mechanisms (git-mode token emission, search-result identity dedupe, and tokenized recent/history records) but found two smaller contract defects that still block commit: git search truncated `scanned` metadata is off by one relative to walk mode, and write-create accepts an invalid `path_token` instead of failing loud.
- The correct next intervention is to repair those two contract defects with tests first, then run one Docker/browser proof that exercises the file path from visible choice to opened file. Browser proof before those source-level mechanisms are fixed is not acceptance evidence.

## Next file/repository/mobile roadmap after D4 path identity
- Once non-UTF openability is closed or bounded, the next actionable product target is repository status/diff browser/mobile certification. The server/API git matrix is not enough; the user-facing surface must render dirty/untracked/renamed/deleted/binary/nested/non-repo states readably and must remain visibly read-only on desktop and 390x844 mobile.
- File-editor capability is decision-gated. Current fail-loud Edit behavior is truthful, but Monaco is not provisioned anywhere, so browser editing is not a real capability. The product must either provision the editor assets and certify edit/save/conflict flows, or retire the editor affordance/scope. This is a product-scope decision, not a bug to patch silently.
- File upload/attachment is the next file-mutation evidence target after repository status/diff: browser proof is missing and `tests/test_file_upload.py` is the remaining PROMPT-named monkeypatch seam.
- Codex/Claude real-answer parity stays parked on credentials/gateway health; the acceptance ledger must keep it explicit rather than pretending Pi certification covers all selectable backends.

## Repository status/diff certification state
- Repository status/diff read-only integrity is supported by browser evidence: desktop and mobile showed no write/stage/commit/checkout controls, and the unavailable Edit path remained fail-loud. API evidence also supports tracked modified/deleted/staged-add/binary baseline behavior and explicit binary/non-repo errors.
- The surface is not yet certified as a complete repository-state surface. Four evidence-backed defects remain: unified diffs are not rendered in the browser when Monaco is unavailable, untracked files are invisible, rename relationships are hidden, and non-repo errors are swallowed into an empty picker. The next intervention belongs to repository rendering/API state, not broad refactor or Monaco provisioning.

## D4 display-collision anomaly
- D4 is still open despite successful single-file token roundtrip. Browser/Docker evidence found a server-side heap ordering crash when two distinct files share the same JSON-safe display path and search score: raw-byte `bad<ff>name.txt` and literal `bad\xffname.txt` both display as `bad\xffname.txt`; `/file/search?q=bad` returns HTTP 500 because the heap compares entry dictionaries after score/display ties.
- This is a narrow search-ranking data-structure bug, not a failure of the token identity model. The next fix should add a comparable identity tie-breaker so both visible choices survive and the browser can present distinct operational identities.

## D4 current state after collision rerun
- Backend/search identity is now supported for display collisions: same-display raw-byte and literal-backslash files both survive search, remain JSON-safe, and open to distinct content through their operational identities. This closes the server-side crash mechanism.
- D4 is not fully user-complete because the picker renders same-display identities with indistinguishable labels. The next D4 intervention is a frontend presentation fix: visible choices with different operational identities but the same display path need a disambiguating hint that preserves the minimal UI and does not invent write semantics.

## Current file/repository state after `4cf7e3c` browser proof
- D4 non-UTF openability is now closed for the known same-display collision mechanism. Docker/browser verification on port 19141 proved `/file/search?q=bad` returns both identities for raw-byte `bad<ff>name.txt` and literal `bad\xffname.txt`; the picker renders visible/title identity hints (`current folder · non-UTF bytes` vs `current folder · literal name`); both choices open the correct distinct content. This supersedes the prior "indistinguishable labels" D4 gap.
- Repository status/diff is now certified for the previously observed read-only rendering gaps. Docker/browser verification at `4cf7e3c` proved changed-files shows tracked stats, untracked badges, binary explicit stats, and rename `oldname.txt → renamed.txt`; Monaco-unavailable diff mode renders unified HEAD-to-working-tree diff text with `Rich diff unavailable. Showing unified diff (read-only).`; non-repo cwd shows `Not a git repository — no changed files`; desktop and 390x844 mobile remain read-only with no stage/commit/checkout controls and no horizontal overflow.
- Source and local validation for the functional patch remain green (`node --check` for 5 changed JS files, focused `241 passed / 81 subtests`, full local `1649 passed / 132 subtests`). The browser verifier's acceptance wrapper failed only because its report omitted a changed-files metadata field; the substantive observations and preserved artifacts support PASS.
- Residual product gaps are now bounded outside D4/repository truthfulness: Monaco/editor assets still do not load in a clean deployment; the Edit affordance is fail-loud/read-only but not a real editing capability; diff-toggle timing can briefly show file content at the Monaco timeout boundary before settling to unified diff. These belong to the editor capability decision, not to the closed D4/repository fixes.

## Plain text editor fallback functional state
- Functional commit `ef83bda` changes file editing from a dead Monaco-only affordance into a source-validated plain-text capability. In clean deployments where Monaco is absent, editable text files render a distinct `plain-edit` textarea surface; `plain-fallback` remains read-only and diff/preview/binary/blocked paths are not made editable.
- The accepted mechanism is frontend-only: existing `/file/write` remains the save authority, with existing version/conflict/lock and non-UTF `path_token` plumbing. `plain-edit` participates in dirty detection, save text capture, cancel/restore, read-only projection during save/unavailable state, and path metadata/ARIA updates when the same editor is reused for another file.
- Source review found no blocker after two corrections: dirty detection must read the current editor kind rather than hardcoded Monaco `file`, and reused plain-edit editors must update path metadata/ARIA. Local validation is green (`node --check` changed JS, focused `96 passed / 25 subtests`, full local `1651 passed / 132 subtests`).
- Browser/Docker proof now supports the user-visible editor capability. Artifact-preserving verifier `44556c1c-3ada-4700-8a71-26a5818268ac` ran in Docker on port 19143 with Monaco absent (`/monaco/vs/loader.js` 404) and preserved DOM/API/screenshot evidence under `browser-artifacts/plain-editor-19143/`. It proved editable text files render a plain textarea, edit/save/reopen writes through `/file/write` and changes version, discard leaves disk unchanged, 409 conflict displays Reload/Keep and Keep preserves draft+dirty state, unavailable sessions preserve a read-only selectable draft with copy-edits status, diff/binary/oversize/non-UTF paths remain read-only, and mobile 390x844 has 16px editable textarea with no horizontal overflow. Reload-from-disk conflict action remains a headless-confirm harness boundary; the conflict row and Keep branch are proven.

## Upload/attachment certification state
- Upload/attachment is now certified for the core product target: browser paperclip upload stages a file in the app-dir uploads area, injects the absolute staged path into a real Pi session, and the agent can read that path and return the sentinel content. Docker/browser certification on port 19144 proved this on the target behavior: Pi issued a `read` tool call on `/home/tester/.local/share/codoxear/uploads/.../sentinel-source.txt` and returned `ATTACH-SENTINEL-1783295830` in the assistant transcript.
- The upload semantic is precise: single-file paperclip upload, not drag/drop, multi-file, paste-to-attach, or cwd import. Files are staged under `~/.local/share/codoxear/uploads/<session>/...` with mode 0600 and the backend receives a text reference `Attachment N: <absolute_path>`. Prior concern that app-dir staging might make files unreadable to Pi is falsified for the certified Docker/Pi environment.
- Browser evidence also supports the important failure/control surfaces: oversize non-image fails loud before staging; attach while busy is blocked/409-visible; invalid API bodies return explicit 400s; pending attachments disable the Later/queue path; pending attachment state survives a fresh second server process and can be cleared; mobile 390x844 attach/send/read-sentinel works.
- Boundaries remain explicit: desktop HEIC decode fails loud and is not claimed cross-platform; mobile paperclip is reachable but measured 34x34, below the 44px ideal; a stale local attached-files badge can linger after reload even when server pending_attachment is false. These are residual polish/impairing items, not blockers for the certified single-file upload claim.
- The product certification made upload test-debt retirement safe: the former `tests/test_file_upload.py` server-global/do_POST monkeypatch seam could be replaced by direct `file_upload` module tests plus injected-deps route tests without changing product behavior.


## Upload test seam retirement state
- Upload test debt is retired at `163320c`: `tests/test_file_upload.py` no longer imports `codoxear.server`, patches server globals, or drives `server.Handler.do_POST`. It directly tests `file_upload.safe_filename`, `stage_uploaded_file` with explicit `upload_dir`/`now_fn`/`max_bytes`, and `attachment_inject_text`.
- Route behavior moved to the injected dependency seam in `tests/test_control_routes.py`: the real `handle_control_post_route` / `_handle_inject_attachment` path now covers happy-path JSON serialization and bracketed paste, auth denial, bad/missing filename/index/base64, oversize/generic staging failures, inject-text validation, readiness 404/409 variants, injection 404/502/504 commit-unknown, and readiness-before-decode ordering.
- Targeted validation for the seam replacement passed (`46 passed` across `tests/test_file_upload.py`, `tests/test_control_routes.py`, and `tests/test_file_upload_module_source.py`), and independent critic `ea863301-48b9-407e-b3ab-edffac4ec769` found no blocker. The critic’s only residual is a non-blocking untested race branch where `session_not_ready_error` can be raised during `inject_attachment_keys`; readiness failures already cover the same user-visible 409 class.

## Current attachment/mobile/transcript certification state
- Current functional HEAD `fb42cfd` is browser/API-certified for the tranche that followed the upload-route seam retirement. Docker/browser artifacts on port 19200 prove the attachment indicator, deleted-upload cleanup, mobile composer tap targets, no-answer transcript projection, and broker-state idle/resume projection through real Codoxear server routes and browser DOM.
- Attachment indicator authority is now supported by product evidence: the badge appears after `/inject_file`, survives reload from server `pending_attachment`, hides immediately after send with `allow_pending_attachment` (observed before the next poll), and hides after `/pending_attachment/clear`. The accepted mechanism is selected-session server pending truth plus direct local attach feedback; session-list refresh remains the server overwrite authority.
- Deleted-session upload cleanup is supported by end-to-end and direct-function evidence: deleting a session removes only `uploads/<session_id>`, preserves sibling upload dirs, and unlinks a symlink entry without following or deleting its outside target.
- Mobile composer ergonomics are supported for the current controls: at 390x844, paperclip, queue, stop, and send measured 44x44 CSS px with no horizontal overflow under both desktop-pointer and touch-emulated browser conditions.
- No-answer transcript projection is supported in API and browser: a Codex-style completed user turn with no assistant output renders the explicit assistant error message `The backend completed this turn without producing a response.`, while a normal answered turn does not receive a false no-answer event.
- Idle/resume projection is supported for the broker-state path: a post-interrupt idle session projects idle, resumed activity after `/send` projects busy, and completion projects idle again in both `/api/sessions` and the sidebar dot. The narrower log-only stale-`interrupted_idle` variant, where a broker keeps reporting `interrupted_idle:true` while log activity advances, remains a verification boundary here; unit coverage supports the guard, but a dedicated Docker/browser harness has not re-driven that exact variant.
- The verifier wrapper reported failure only because its acceptance metadata lacked changed-files evidence; the preserved report and artifacts contain the product observations above, and no code edits were made by the verifier.
- Next work should not re-open this tranche unless new evidence contradicts it. The next user-facing uncertainty is either to close the log-only interrupted-idle browser proof or move to the next surface-map gap with evidence-backed defects, especially remaining mobile/file/repository controls or backend-parity boundaries that affect visible results.


## Next target: Claude Code turn-outcome transcript truth
- Fresh-context target analysis identified a user-visible projection gap outside the current idle-verification thread: the no-response invariant is currently enforced for Codex-shaped closes, but Claude Code turn outcomes can close through `system` rows that transcript projection ignores.
- The live mechanism to test/fix is narrow: a CC `user` row followed by `system/subtype:turn_duration` with no assistant text, or a terminal `system/subtype:api_error` without a trailing assistant error row, can leave the browser with a user prompt and idle session but no visible answer/error/no-response message. This violates the selectable-backend promise without depending on real CC credentials.
- The next implementation should be discriminator-first: reproduce the silent CC close shapes through `_read_chat_live_delta`/tail tests, then project terminal CC no-answer/API-error outcomes as error-class transcript messages while preserving answered CC, Codex, and Pi behavior. Transient CC retry rows must not each become transcript errors.
- This target is disjoint from the in-flight log-only stale-`interrupted_idle` verifier: transcript projection lives in `rollout_chat_events`/`cc_log`/backend chat-event wiring, while the idle verifier owns `session_runtime`/`session_listing`/broker state.


## Stale interrupted-idle listing defect resolved across prune and discovery refresh
- Docker/API certification falsified the previous unit-only belief: stale `interrupted_idle=true` can be refreshed before log-counter update, causing the listing to project idle while the same log contains resumed activity. The first fix `8e5bae8` corrected broker/prune repeated-true by preserving the original interrupt baseline and suppressing stale true after post-interrupt activity.
- Clean-room review found the remaining authority bypass: `SessionDiscoveryRegistryCoordinator.upsert_registration()` directly re-baselined `previous.interrupted_idle_log_off` to discovery's current `meta_log_off`. Functional commit `f5b4710` routes discovery refresh through `set_session_interrupted_idle()` so discovery, prune, and broker refresh share one baseline/suppression authority.
- Failing evidence under `browser-artifacts/stale-interrupted-idle-discovery-race-fail/` shows the previous discovery-first timing projected phase-2 resumed activity as `busy=false`; fixed evidence under `browser-artifacts/stale-interrupted-idle-discovery-race-fixed/` forces the same timing and shows phase 2 as `busy=true` across five polls, then phase 3 completion as `busy=false`.


## Claude Code terminal outcome projection state
- Functional commit `f721dad` closes the Claude Code `system`-row blind spot for transcript outcomes. A CC user turn that closes via `system/subtype:turn_duration` with no assistant text now emits the shared no-response assistant error; a terminal `system/subtype:api_error` with exhausted retries emits the row's real error text as an assistant error; transient retry rows remain silent by themselves; answered CC turns remain answered without synthetic no-response.
- The fix also aligns CC visible error outcomes with busy/idle reduction: terminal `system/api_error` closes the current turn in `cc_current_turn_state_before`, so a visible error transcript row does not leave the log reducer thinking the turn is still busy.
- Docker/browser certification on port 19210 supports the user-visible claim through real server routes and DOM rows: `cc-noresp` rendered the no-response error, `cc-apierr` rendered `API Error: 503 Service Unavailable` as `.msg assistant error`, and `cc-normal` rendered `CC-ANSWER-OK` as a normal assistant message.
- Boundary: this is deterministic CC log projection proof, not real Claude inference parity. Credentials/gateway health remains parked separately.


## Mobile file touch dpad resolved
- Functional commit `ff9962f` raises the file viewer touch dpad controls (`.fileTouchBtn`) from the desktop/base 34px grid to a mobile-only 44px grid under the file viewer. The desktop compact dpad remains 34px; the mobile override owns grid tracks, spacers, button width/height, and min size together so buttons fill cells instead of overflowing a smaller grid.
- Browser evidence on port 19220 supports the mobile ergonomics claim: before simulation measured all seven dpad/action controls at 34x34; after the fix, up/left/down/right/select/copy/paste measured 44x44, the dpad grid computed to 44px tracks, the toolbar stayed 368px wide inside a 390px viewport, and body scroll width equaled viewport width.
- Boundary: clean Docker lacks Monaco activation for the touch toolbar, so the certification force-displayed the toolbar DOM and measured CSS layout. Existing file viewer/editor behavior tests continue to cover activation/update behavior.


## Final clean-room blockers resolved: live CC split route and fresh interrupted-idle discovery
- Clean-room review of HEAD `3427fef` found two remaining user-visible contract gaps: the public `/messages/live` route used Codex-only prior turn context for split live polling, and fresh discovery insertion cleared `registration.interrupted_idle` via `reset_log_caches()` before storing the new session.
- Functional commit `2506938` makes the public live route use backend-aware `_prior_open_turn_context()`, so a Claude Code user row in one poll and `system/subtype:turn_duration` close in the next poll emits the shared no-response assistant error. Functional commit `b858bfd` re-applies `set_session_interrupted_idle()` after fresh discovery cache reset when the broker reports interrupted idle, so fresh registry insertion preserves the override baseline.
- Docker/API proof under `browser-artifacts/final-blockers-fixed-19234/` supports both fixes through real server routes: `cc-live` tail returned only the user row, then live poll after appended close returned the assistant no-response error; `fresh-interrupt` listed `busy:false` on first discovery over a non-final log with broker `interrupted_idle:true`.


## Accepted checkpoint: visible outcomes, stale interrupted-idle, and mobile file dpad
- Clean-room critic `46181ecc` accepted HEAD `415e46f` with no blocker-grade contradiction after final local/Docker validation. The accepted tranche includes Claude Code terminal outcome projection, public split-live CC no-response projection, stale interrupted-idle correctness across prune/discovery-refresh/fresh-discovery paths, attachment indicator/deleted-upload/mobile composer/no-response evidence from the prior tranche, and mobile file dpad 44px target sizing.
- The current supported product claim is deterministic browser/API truthfulness for these mechanisms in isolated Docker: visible result rows are emitted for the certified no-answer/error paths, binary busy/idle no longer stays falsely idle or falsely busy for the tested interrupted-idle races, and the certified mobile controls meet target-size/no-overflow constraints.
- Boundaries remain explicit: real Claude/Codex inference parity depends on provider credentials/gateway health; final split-live proof is API-level paired with earlier DOM error-row proof; mobile dpad proof certifies CSS layout under forced toolbar visibility because Monaco activation is unavailable in clean Docker.


## Transcript search misses synthetic no-response rows
- Read-only scout `88f4fba2` found a narrow transcript-surface defect after the accepted visible-outcome tranche: `/messages/search` does not include synthetic no-response rows, while tail, history pagination, live polling, and fresh re-read do preserve them.
- Mechanism: `transcript_search.iter_positioned_chat_events_forward()` builds search events with `_single_chat_event()` per log row and never runs `_inject_no_response_events()`. The no-response text is created only by `_extract_positioned_chat_events()`, which tail/history/live use. Therefore Codex `user_message` + `task_complete` no-assistant and Claude Code `user` + `system/turn_duration` no-assistant rows render in the transcript but are not searchable and carry no search cursor.
- Scope is narrow: ordinary search still works, and terminal CC `system/api_error` text is searchable because it is projected directly from the log row. The fix should route search through the same positioned no-response-injecting event stream while preserving `_before_byte`/`_after_byte` cursor semantics and answered-turn suppression.


## Transcript search synthetic no-response rows resolved
- Functional commit `06930c9` makes transcript search use the same no-response-injecting positioned event semantics as tail/history/live. Search now emits synthetic no-response rows for Codex no-answer closes and Claude Code `system/turn_duration` closes, while answered turns still suppress the generic row and terminal CC `system/api_error` text remains searchable as real backend text.
- Cursor mechanism is supported: injected no-response matches carry `_before_byte` at the close record start and `_after_byte` at the close record end, so `/messages/search` attaches `history_cursor` and `load_cursor`; loading history at the load cursor re-injects and displays the same row.
- Docker/API/browser evidence on isolated port 19242 supports the user-facing claim. Both `search-codex-noresp` and `search-cc-noresp` returned `match_count=1` for the no-response phrase, history-loaded windows contained the row, and the browser search UI showed `1/1 loaded · 1 all` with the rendered row highlighted. Answered Codex no-response search returned zero; real Codex answer and CC terminal API error searches returned one.
- Residual internal risk: search now buffers the bounded record window before applying `count_limit`, so `count_max` no longer bounds record consumption. Independent audit judged this non-blocking because it changes resource behavior, not transcript/search semantics. If large-log search performance becomes visible, build a streaming injector without reintroducing a second no-response authority.


## Transcript search accepted checkpoint
- Clean-room critic `0818468c` accepted the committed transcript-search tranche (`bb7d38d`) with no blockers. The accepted claim is now: rendered synthetic no-response rows for Codex and Claude Code are searchable, carry cursors, and rehydrate through history as the same row.
- The critic preserved the same two boundaries as the main evidence: `count_limit` record-consumption is now a resource risk rather than a semantic failure, and deterministic fake logs prove parser/search projection rather than live provider parity.


## Log-only stale interrupted-idle boundary closed
- Docker/API/browser verification on port 19250 closes the prior residual boundary: a broker socket kept returning `busy:false, interrupted_idle:true` while post-interrupt activity was appended to the same log. The first listing projected the immediate interrupted non-final tail as idle (`busy:false`), then after a post-baseline `event_msg/user_message`, five repeated `/api/sessions` polls projected `busy:true` while direct broker socket probes still returned `interrupted_idle:true`.
- Browser evidence matched the API mechanism: the sidebar state dot changed from idle gray to busy blue and stayed busy on repoll while the fake broker remained stale-true. Optional phase 3 proved clearing broker false resets suppression, and a later genuine interrupt can re-arm the override before being suppressed again by later post-baseline activity.
- Accepted mechanism: `set_session_interrupted_idle(true)` preserves the original interrupt log baseline; listing refresh may see stale true, but `SessionLogRuntimeCoordinator.update_meta_counters()` reads from the baseline, observes post-interrupt user/assistant activity, calls `suppress_session_interrupted_idle()`, and future stale true reports cannot re-enable the override until broker false or log/session reset.


## Log-only stale interrupted-idle accepted checkpoint
- Clean-room critic `ce77e902` accepted the committed log-only stale interrupted-idle proof (`3f0a886`) with no blockers. The accepted claim is narrow and strong: for a same-log post-interrupt resume while the broker socket keeps reporting stale `interrupted_idle:true`, `/api/sessions` and the sidebar project busy and keep doing so across repeated polls.
- Boundaries remain: this proof covers session listing/sidebar projection, not every downstream busy-derived control, and it uses deterministic fake broker/log conditions rather than a real provider interrupt. That is the right discriminator for the stale-true mechanism.


## Readiness paths diverge from stale interrupted-idle suppression
- Docker/API evidence on port 19260 confirms a new blocker-grade authority split: after listing/sidebar suppress stale `interrupted_idle:true` and project `busy:true`, direct send and queue promotion still rebuild readiness from the raw broker state. Because the raw broker response still contains `interrupted_idle:true`, `SessionReadinessCoordinator.runtime_status_from_state_and_log()` treats the non-idle log as remotely ready.
- User-visible consequence: the sidebar can say the session is busy while `/send` still delivers input. In the discriminator, POST `/api/sessions/cert-stale-interrupt/send` returned HTTP 200 and the broker logged `cmd:send` with text `probe direct send while sidebar busy`. A fresh queue discriminator likewise promoted and sent `queue probe on fresh busy session` instead of leaving it queued.
- Mechanism: `SessionControlCoordinator.get_state()` applies `set_session_interrupted_idle()` to stored session state but returns the raw broker response. Listing uses stored suppressed `interrupted_idle=false`; readiness uses `broker_runtime_state(raw_state)` and reactivates `allows_interrupted_idle_override`. Fix should make readiness consume the same suppression-aware interrupted-idle authority as listing before deciding direct send, queue promotion, attachment injection, or unattended injection.
