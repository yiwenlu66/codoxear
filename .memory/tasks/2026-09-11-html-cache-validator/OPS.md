# OPS

## 2026-09-11T15:05:00+08:00

- Task initialized from template. Read prior task memory `.memory/tasks/2026-09-10-inline-subagent-lines/` (PROMPT/OPS/EPISTEMIC), AGENTS.md, and project memory headers.
- Working tree state at start: HEAD `228cb1ef`. Concurrent unrelated modifications (voice/notification + app.css + wiring/tests) present and excluded from this task: `.memory/project/ISSUES_TRACKER.md`, `codoxear/static/app.css`, `codoxear/static/app_application_composition.js`, `codoxear/static/app_notifications.js`, `codoxear/static/app_wiring.js`, `tests/test_frontend_notifications_module.py`, `tests/test_frontend_toast_routing.py`, `tests/test_frontend_voice_module_source.py`, `tests/test_notification_feed_all_backends.py`, `tests/test_notifications.py`, `tests/test_voice_settings_save.py`, plus untracked logo artifacts.
- Defect mechanism investigation (read-only host evidence, no live HTTP probes):
  - `codoxear/static_routes.py::send_static_file` computes `etag = W/"{st_mtime_ns:x}-{st_size:x}"` from the raw file stat BEFORE `deps.read_static_bytes(path)` substitutes `__CODOXEAR_ASSET_VERSION__` (content hash of all frontend assets) and `__CODOXEAR_ATTACH_MAX_BYTES__` into HTML.
  - Serving root on the live deployment is the deploy worktree, not the pipx venv: the service runs `WorkingDirectory=/home/yiwen/.local/share/codoxear/deploy` with `-m codoxear.server`, and `python -m` prepends CWD to sys.path, so the package imports from the worktree. `git checkout` between commits rewrites only files whose content changed → index.html keeps its checkout-era stat across releases whenever its bytes are unchanged.
  - Evidence: deploy worktree `index.html` mtime `2026-09-10 12:03:01` (predates the 09-11 a206/5d62 deploys); `git diff` shows index.html byte-identical a206ca5d→5d62d020 and fda16968→5d62d020; pipx venv site-packages has index.html mtime = deploy time (14:12:08) but is never the import root. Also observed: the built wheel omits `static/dist/` entirely (package-data lists no `static/dist/*`), so if the unit ever lost `WorkingDirectory` or the venv console script were used directly, the bundle would 404 — separate latent packaging issue, explicitly out of scope for this task (reported as deployment-scope risk).
  - Therefore on the real release path: asset-only release (a206→5d62 changed app_transcript.js/app.css/bundle only) changes the served HTML (?v= V1→V2) while index.html stat and thus the stat ETag stay identical → conditional GET returns 304 → browser reuses old HTML with old immutable ?v= URLs → old bundle+CSS from cache → user sees the old displaced subagent activity layout after a normal reload. Attachment-limit placeholder is a second trigger (env change + restart, no file change at all).
  - Service worker ruled out as a staleness mechanism: `codoxear/static/service-worker.js` registers only `push`/`notificationclick` handlers, installs no fetch handler, caches no shell.
- Reproduction plan (must run before source edits): Docker container with a host-side /tmp static tree copy; mutate only asset bytes; recreate container (fresh process clears the `static_asset_version` lru_cache, mirroring the service restart); index.html untouched → identical stat. Assert (pre-fix): plain GET body's `?v=` changed while `If-None-Match` with the old ETag returns 304. Browser scenario: same agent-browser profile loads release A (a206ca5d archive), release swap on the same port with `cp -p` index.html (stat preserved), normal `location.reload()`; buggy new release keeps old UI, fixed new release updates.

## 2026-09-11T15:40:00+08:00

- Reproduced the defect FIRST (pre-fix HEAD `228cb1ef`), Docker-isolated, real HTTP conditional requests. Harness: `/tmp/codoxear-cache-repro-sh/repro.sh` (uncommitted); serving root = host `/tmp` tree copy mounted at `/srv` with `--workdir /srv` (mirrors the live `python -m` + WorkingDirectory import semantics); release swap mutates only `app.css` bytes; process restart = new container (fresh `static_asset_version` lru_cache); `index.html` never touched.
- Pre-fix evidence (artifacts `/tmp/codoxear-cache-repro-run1/`, structured verdict `/tmp/codoxear-cache-repro-run1b/artifacts/report.json`):
  - Phase 1 baseline: `GET /` → 200, `ETag W/"18d3d6421fbb8f17-261d"`, body `?v=839ead95b69c`, attach limit 16777216; `/static/index.html` identical ETag; gzip variant identical ETag; unchanged revalidate → 304.
  - Phase 2 asset-only release (index stat verified byte-identical across phases): body version changed to `c1437424fed3`, ETag UNCHANGED → `If-None-Match` with phase-1 ETag → **304** on both `/` and `/static/index.html` (the defect: same validator, different representation).
  - Phase 3 attachment-limit-only release (no file change, only `CODEX_WEB_ATTACH_MAX_BYTES` 16777216→8388608): body limit changed, ETag unchanged → conditional → **304** (second defect trigger).
  - Non-HTML controls: `app.css?v=` immutable header preserved; unversioned `app.css` stat ETag revalidates 304; gzip/identity share the same weak ETag.
- Implemented the fix in `codoxear/static_routes.py::send_static_file` only: HTML responses compute `etag = W/"<sha256(final substituted bytes)>"` from one `read_static_bytes` call shared by the 304 decision and the 200 body (single read per request); non-HTML keeps the stat fast path (`W/"{st_mtime_ns:x}-{st_size:x}"`, 304 without reading); `Cache-Control` policy untouched; hash taken before content coding so weak identity is shared across gzip/identity.
- Post-fix Docker rerun (`/tmp/codoxear-cache-repro-run2/artifacts/report.json`, verdict OK): asset-only release → conditional 200 with new `?v=` (ETag `W/"0556…"` content hash); attach-only release → 200; unchanged representation → 304 immediately and after restart; gzip revalidate → 304 with identical ETag; non-HTML stat ETag + immutable policy unchanged.
- Added executable tests `tests/test_static_html_cache_validators.py` (8 tests, real `handle_static_get_route` execution): asset-change-with-frozen-index-stat → 200 (and validator equals sha256 of served bytes), attach-limit-only → 200, unchanged representation → 304 even when mtime moves (content-derived), bare index edits → new validator + 200, `/` and `/static/index.html` share one validator, weak ETag shared across gzip/identity with coherent cross-encoding 304, exactly one HTML read per request (200 and 304), non-HTML validator stays stat-based (mtime-only move refreshes it) with immutable policy preserved. Docker focused gate: 23 passed (new file + `test_static_routes.py` + `test_static_assets.py`); wider gate incl. `test_server_http/traffic_floor/continuous_traffic_budget/auth_cookie/brand_assets/shipping_mechanisms_e2e/sse_battle_advanced`: 46 passed.
- Full Docker suite: `1894 passed, 112 subtests passed`, 3 failed — the three pre-existing unrelated failures documented in prior OPS entries (2× PDF.js requires newer Node than sandbox Node 18; 1× deploy-script clean-snapshot test rejecting the intentionally dirty shared checkout). No test touching static routes or cache validators failed.
- Browser upgrade proof (harness `/tmp/codoxear-cache-repro-sh/scenario.sh`, uncommitted; artifacts under `/tmp/codoxear-cache-upgrade-*`): release A = git archive `a206ca5d`; release B = git archive `5d62d020` (buggy mode) or `5d62d020` + fixed `static_routes.py` only (fixed mode); `index.html` verified byte-identical and `cp -p` stat-preserved across the swap; ONE persistent agent-browser profile per scenario (fresh at A, same profile through the release + `location.reload()` — no fresh profile substituted at B); viewport 606×800; fixture = existing `tests/fixtures/subagent_activity_broker.py` (2 parallel children).
  - Buggy (`/tmp/codoxear-cache-upgrade-upbuggy7/artifacts/`): A idle label `▸2 subagents working`, marker→label gap 90px (a206 displaced grid), nav 200 transfer 3682. After release + normal reload: nav `responseStatus 200` but `transferSize 300` with `encodedBodySize 3382` (header-only revalidation: server answered 304 and Chrome composed the cached body), asset version unchanged `de9773dcf75d`, `app.css?v=` and `dist/app.bundle.js?v=` served from immutable cache (`transferSize 0`, no request), idle bubble STILL displaced gap 90px — the user's exact symptom reproduced end-to-end.
  - Fixed (`/tmp/codoxear-cache-upgrade-upfixed2/artifacts/`): same A. After release + normal reload: nav 200 with `transferSize 3682` (full document), asset version `5b5e81a3f3a9` (changed), new `app.css?v=5b5e81a3f3a9` fetched 18737 bytes and new `dist/app.bundle.js?v=5b5e81a3f3a9` fetched 175865 bytes, idle label `▸2 subagents working` beside its marker with gap **8px**, browser errors empty. Screenshots `phase-A.png`/`phase-B.png` retained in both artifact dirs.
- AGENTS.md Performance bullet updated: HTML ETag is content-hash based (reason: asset-only releases leave index stat unchanged), non-HTML keeps stat validators.
- No deploy performed. Concurrent voice/notification/CSS/test modifications untouched and unstaged.
