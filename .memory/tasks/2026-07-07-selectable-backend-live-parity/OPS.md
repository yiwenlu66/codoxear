# OPS

- 2026-07-07T07:40:00Z — Initialized selectable-backend live parity task. Mechanism under test: backend tabs are browser product promises; a browser-created Claude Code session must either launch/bind/send/render a truthful outcome or fail visibly with failed-row semantics. Silent idle/disappearing backend rows are not acceptable outcomes.
- 2026-07-07T15:45:00Z — Ran selectable-backend live parity proof on Docker port 19396. Browser New Session flow selected Claude, model `sonnet`, reasoning `max`, direct launch from `/workspace`. Docker lacks a usable Claude executable (`command -v claude` empty; launch PTY tail reports FileNotFound for `/usr/games/claude`), producing visible failed-launch branch: `/api/sessions` row `session_id=launch-1783410079933-781f85a0`, `agent_backend=cc`, `launch_state=failed`, `launch_stage=broker_early_exit`, `model=sonnet`, `reasoning_effort=max`, `busy=false`, `log_path=null`; `/messages/tail` returns transcript_state `failed` with one assistant error row. Browser selected failed row rendered recovery panel with stage and launch settings; send/queue/attach/file controls disabled; API send/enqueue/file-list/attachments returned 404. Artifacts: `.memory/tasks/2026-07-07-selectable-backend-live-parity/browser-artifacts/backend-parity-19396/`.
- 2026-07-07T16:05:00Z — Clarified proof artifacts in `9195f5c Clarify selectable backend proof snapshot`: `snapshot-after-start-3s.txt` is intermediate/ambiguous and not used as failed-state proof. Failed-state proof rests on selected failed-row browser snapshot/evals, API failed row/tail, and runtime launch-ledger evidence.
- 2026-07-07T16:10:00Z — Clean-room review completed and committed as `3abc5b6 Record selectable backend parity review`. Verdict: accept with nonblocking findings. No blockers. Residuals: usable Claude success branch remains unproven until a configured Claude executable/auth environment exists; raw `provider_choice="openai-api"` on failed CC API row is cosmetic while Details shows `Provider: -`.
- 2026-07-07T16:15:00Z — Updated task/project memory. Current accepted failed-launch branch invariant: a browser backend-tab launch that exits before log bind must remain visible as a synthetic failed-launch row preserving backend/model/reasoning settings, expose local recovery details, and block all real-session actions in browser and API.
- 2026-07-07T08:16:42Z — Ran deterministic usable-branch proof on Docker port 19397 with a container-only fake `/usr/local/bin/claude` (explicitly labeled `FAKE_CLAUDE_CODE_FOR_CODOXEAR_DOCKER_ONLY`; no host credentials/config). Actual browser New Session flow selected Claude tab, model `sonnet`, reasoning `max`, direct launch (`tmux=false`), cwd `/workspace`, session name `fake claude usable proof`. Bound usable row: `session_id=broker-190`, `thread_id=69121efb-477b-47da-a1e2-bd10cb85aafd`, `agent_backend=cc`, `transport=null` (direct sidecar/ledger transport `direct`), `log_path=/home/tester/.claude/projects/-workspace/69121efb-477b-47da-a1e2-bd10cb85aafd.jsonl`, `busy=false`, `launch_state=null`, `model=claude-sonnet-4-5`, `reasoning_effort=max`, token `tokens_in_context=4500`, `context_window=200000`, `percent_remaining=98`. Browser sent `USER_SENTINEL_FAKE_CC_PROMPT_19397...`; fake command log records that exact prompt via broker PTY; `/messages/tail` and browser transcript show `FAKE_CLAUDE_ASSISTANT_SENTINEL_usable_branch_19397...` as a final assistant response. Browser controls in idle real-session state: send/file/attach/capture/queue/unattended enabled; `#ctxChip` text/title `Ctx 98%` and `Context input: 4500/183616 tokens (16384 reserved; window 200000).` agree with API token. Sidecar metadata has `agent_backend=cc`, `model=sonnet`, `reasoning_effort=max`, live `log_path`, and same session/thread identity. Launch ledger records `starting` and `broker_spawned` for `cc`/`sonnet`/`max`/`direct`; current successful launch ledger does not append a separate log-bound success row. No product-code fix needed. Focused tests `tests/test_cc_log.py tests/test_cc_session_log.py tests/test_backend_launch_adapter.py` passed (26 passed). `git diff --check` passed. Exact sandbox stopped. Sanitized proof artifacts committed in `b59cf4c Record selectable backend usable fake Claude proof` at `.memory/tasks/2026-07-07-selectable-backend-live-parity/browser-artifacts/backend-parity-fake-cc-19397/`.
- 2026-07-07T08:35:16Z — Clean-room usable fake-Claude review completed and committed as `d76981f Record usable fake Claude review`. Verdict: ACCEPT with zero blockers. Review confirmed seven assertions: browser selected Claude/sonnet/max/direct, bound real CC row rather than failed/synthetic row, broker PTY prompt reached fake CLI, final assistant sentinel rendered in browser and `/messages/tail`, token values agreed across `/api/sessions`/tail/`#ctxChip`, idle real-session controls enabled, and proof boundary excluded real Claude auth/schema/tool/provider behavior. Nonblocking findings: raw `provider_choice="openai-api"` on CC rows, CC defaults list `fable`, `poll-after-start.jsonl` has pre-auth 401 noise, one zero-byte click marker, oversized control inventory, and mid-send vs idle snapshot naming difference.
