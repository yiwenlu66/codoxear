# Operations evidence

- 2026-08-04T10:14:40Z — Existing deployed server at `127.0.0.1:8743` returned `200` for `/` and `401` for unauthenticated `/api/sessions`.
- 2026-08-04T10:15:00Z — Authenticated session list contained three pre-existing busy sessions. Supervisor directed use of a temporary web-owned Pi session and explicitly prohibited prompt injection into shared active sessions.
- 2026-08-04T10:24:00Z — Real browser capture, using the temporary session selected by `#session=broker-2187362`, lasted 61.38 seconds. It recorded 72 total requests / 410,941 wire bytes including cold assets; continuous API traffic was 24 requests / 73,318 bytes. `/api/sessions` was 17 requests (11×200, 6×304; 70,130 wire bytes), while transcript tail was one 1,391-byte request and SSE delivered one 881-byte stream. Voice settings and notification subscription each ran twice (1,472 and 608 wire bytes respectively). No fallback message-tail polling occurred because SSE stayed open.
- 2026-08-04T10:27:00Z — Targeted behavioral validation passed: 37 tests covering listing, run-settings backfill, and session-list coordination.
- 2026-08-04T10:28:00Z — Committed backend/test checkpoint `f48a34b9` (`Avoid unchanged log settings scans in session polls`) using explicit paths only.
- 2026-08-04T10:31:00Z — Full requested suite ran: 1,487 passed, 103 subtests passed, 24 failed. All failed cases belong to concurrent frontend changes in `tests/test_diagnostics_layout_css.py` and `tests/test_frontend_unattended_module_source.py`; targeted backend/listing validation remains 37 passed.
- 2026-08-04T10:32:00Z — Deleted temporary web-owned probe session `broker-2187362` through `POST /api/sessions/broker-2187362/delete`; no prompt was sent to it or to the three pre-existing busy sessions.
- 2026-08-04T10:30:00Z — `scripts/deploy.sh f48a34b9` installed the committed snapshot and restarted only the server, but its browser boot check failed: the committed `app.js` requires `window.CodoxearSse` and the untracked `app_sse.js` is absent from the snapshot. HTTP root became healthy after startup, but UI renders only Reload. This is an unrelated concurrent frontend release defect; do not treat the resulting post-deploy capture (login only) as a performance measurement.
- 2026-08-04T10:35:00Z — After `app_sse.js` was committed by its owner, deployed current HEAD (which includes `1ea5b5d8`). The boot check still failed for a distinct frontend regression: manual login rendered shell chrome but never requested `/api/sessions`, leaving all selected-session controls disabled. A second 61.43-second capture consequently contained only root/auth traffic and is invalid as an after measurement. Deleted a second temporary probe session `broker-2369528` through its documented delete API.
- 2026-08-05T02:21:42Z — Audited remaining continuous transports without accessing the live deployment. Existing route coverage confirmed EOF live polling does not reopen/parse JSONL. Added route coverage proving two unchanged tail opens invoke the bounded reverse scanner once.
- 2026-08-05T02:21:42Z — Coalesced the VoicePush-owned settings and subscription snapshots into `/api/settings/voice`; the focused voice controller consumes that snapshot and skips secondary refreshes when both local voice announcements and browser notifications are disabled. Visibility resume now reuses an in-flight SSE connection and starts fallback polling only when SSE cannot be opened.
- 2026-08-05T02:21:42Z — Added deterministic 60-second warm-cache idle-session traffic contract: 25 API/stream opens and 13,260 budgeted bytes (fixture response bytes plus a 512-byte per-exchange HTTP envelope), below 30 requests and 50 KiB. The test exercises production polling/API helpers; it has 18 session polls (17 cached 304), one tail, one SSE open, two aggregate settings calls, two feeds, and zero subscription snapshot calls.
- 2026-08-05T02:21:42Z — Focused validation command `/home/yiwen/.local/share/pipx/venvs/codoxear/bin/python -m pytest -q tests/test_continuous_traffic_budget.py tests/test_voice_routes.py tests/test_frontend_voice_module_source.py tests/test_frontend_message_flow_controller.py tests/test_message_routes.py` passed: 43 tests.
- 2026-08-05T02:25:00Z — The current visible session poll policy is 5 seconds, so the executed 60-second fixture floor is 19 API/stream opens and 10,188 budgeted bytes: 12 sidebar requests (1×200, 11×304), bootstrap/tail/SSE once, and voice/feed twice. The 60-second contract remains below 30 requests and 50 KiB.
- 2026-08-05T02:25:00Z — Additional focused traffic validation passed: `/home/yiwen/.local/share/pipx/venvs/codoxear/bin/python -m pytest -q tests/test_traffic_floor.py tests/test_continuous_traffic_budget.py tests/test_voice_routes.py tests/test_frontend_voice_module_source.py tests/test_message_routes.py` (40 passed).
