# OPS

2026-07-07T15:30:00Z Initialized copy/export too-large messaging slice. Prediction: bounded frontend handling is sufficient because the server already returns discriminating evidence (`status=413`, `max_bytes`) for oversized transcript export; the risk is misclassifying unrelated 413/upload errors or weakening the export guard.

2026-07-07T15:31:00Z Confirmed frontend `api()` throws `Error` objects with `status` and `obj`; oversized export route already returns 413 with `obj.max_bytes` and an error string containing `transcript log is too large to export`. Implemented the UI projection in `app_conversation_copy.js` and routed `copyConversation()` through `copyConversationFailureToast()` so only this discriminating shape bypasses the generic copy-failed message. Added active-session route coverage for 413/max_bytes; existing missing-recovery route coverage remains.

2026-07-07T15:32:00Z Validation: `python3 -m pytest -q tests/test_frontend_conversation_copy_source.py tests/test_message_routes.py::test_messages_export_active_session_retains_size_cap tests/test_message_routes.py::test_messages_export_missing_recovery_session_retains_size_cap tests/test_transcript_export.py::TestTranscriptExport::test_export_rejects_oversized_logs_instead_of_truncating tests/test_transcript_export.py::TestTranscriptExport::test_ui_has_copy_conversation_action` passed (11 passed in 0.73s). `git diff --check` passed with no output.

2026-07-07T15:40:00Z Proof artifact directory: `.memory/tasks/2026-07-07-copy-export-too-large-message/browser-artifacts/export-too-large-19464/`. Docker focused validation on port 19461 passed the focused subset (`11 passed`); Docker smoke on port 19462 returned pre-login `/api/me=401`, post-login `/api/sessions=200`, and container app dir `/home/tester/.local/share/codoxear`. Browser proof used exact container `codoxear-export-too-large-19464` on port 19464 with `CODEX_WEB_TRANSCRIPT_EXPORT_MAX_BYTES=1024`, created a runtime-only 2285-byte transcript log, selected `export-too-large-session`, clicked the real `#copyConversationBtn`, observed toast `Conversation too large to copy (max 1 KiB). Use search or copy a smaller range.`, verified no generic `copy failed` wording, clipboard write count `0`, direct export status `413`, `max_bytes=1024`, and exact container cleanup via `docker rm -f codoxear-export-too-large-19464`. The lowered cap avoids committing a bulky >50MiB proof log; unit tests cover default 50MiB formatting.

2026-07-07T15:45:00Z Clean-room critic `8e3f778c-bf6d-441e-9dbb-a33779d10016` returned VERDICT: ACCEPT, no blockers. Review verified the three-gate oversized-export discrimination (`status=413`, `obj.max_bytes`, transcript/export-too-large text), unchanged server cap/route shape, generic failure preservation, lowered-cap browser proof validity, and absence of committed oversized/secret artifacts. Non-blocking concerns: fractional MiB limits would render as KiB near the threshold, and the committed proof artifacts could have been trimmed further but are small. Review artifact copied to `reviews/copy-export-too-large-cleanroom-review.md`.

2026-07-07T15:46:00Z Accepted copy/export too-large messaging slice. Rewrote `EPISTEMIC.md` to the accepted mechanism and promoted the durable product invariant to `.memory/project/ARCHITECTURE.md`: known transcript export-size 413/max_bytes responses should render a conversation-too-large toast, not generic copy/clipboard failure, while preserving the export cap and generic handling for unrelated failures.
