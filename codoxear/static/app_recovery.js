(function () {
  "use strict";

  // Lifecycle failures are transcript events supplied by the session API.
  // This retained asset keeps static-resource compatibility while deliberately
  // providing no separate recovery UI controller.
  window.CodoxearRecovery = Object.freeze({});
})();
